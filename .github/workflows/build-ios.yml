name: Build iOS IPA

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Xcode 26.6
    runs-on: macos-26
    timeout-minutes: 30
    env:
      XTOOL_VERSION: "1.17.0"
      XTOOL_SHA256: 7796364f6b568f3a728ec4afef1d6ccd4b12bdfbca1b33a92cbead65545ed2fe

    steps:
      - name: Check out source
        uses: actions/checkout@v6

      - name: Select Xcode 26.6
        shell: bash
        run: |
          set -euo pipefail
          sudo xcode-select --switch /Applications/Xcode_26.6.app/Contents/Developer
          xcodebuild -version
          swift --version
          test "$(xcodebuild -version | sed -n '1p')" = "Xcode 26.6"

      - name: Install pinned xtool
        shell: bash
        run: |
          set -euo pipefail
          archive="$RUNNER_TEMP/xtool.app.zip"
          url="https://github.com/xtool-org/xtool/releases/download/$XTOOL_VERSION/xtool.app.zip"
          curl --fail --location --retry 3 --output "$archive" "$url"
          echo "$XTOOL_SHA256  $archive" | shasum --algorithm 256 --check
          ditto -x -k "$archive" "$RUNNER_TEMP"
          echo "$RUNNER_TEMP/xtool.app/Contents/Resources/bin" >> "$GITHUB_PATH"
          test "$($RUNNER_TEMP/xtool.app/Contents/Resources/bin/xtool --version)" = "xtool $XTOOL_VERSION"

      - name: Test shared counter logic
        run: swift test

      - name: Generate Xcode workspace
        run: |
          set -euo pipefail
          xtool dev generate-xcode-project
          test -d xtool/AppbaseIOS.xcworkspace
          xcodebuild -workspace xtool/AppbaseIOS.xcworkspace -list

      - name: Build with Xcode
        shell: bash
        run: |
          set -euo pipefail
          xcodebuild build \
            -workspace xtool/AppbaseIOS.xcworkspace \
            -scheme AppbaseIOS \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -derivedDataPath "$RUNNER_TEMP/DerivedData" \
            CODE_SIGNING_ALLOWED=NO

      - name: Embed Xcode-generated App Intents metadata
        shell: bash
        run: |
          set -euo pipefail
          products="$RUNNER_TEMP/DerivedData/Build/Products/Release-iphoneos"
          generated_metadata="$products/AppbaseIOS.appintents/Metadata.appintents"
          embedded_metadata="$products/AppbaseIOS.app/Metadata.appintents"

          test -s "$generated_metadata/extract.actionsdata"
          ditto "$generated_metadata" "$embedded_metadata"
          test -s "$embedded_metadata/extract.actionsdata"

      - name: Verify, ad-hoc sign, and package IPA
        shell: bash
        run: |
          set -euo pipefail
          app="$RUNNER_TEMP/DerivedData/Build/Products/Release-iphoneos/AppbaseIOS.app"
          widget="$app/PlugIns/AppbaseIOSWidget.appex"
          metadata="$app/Metadata.appintents/extract.actionsdata"

          require_executable() {
            local label="$1" path="$2"
            printf '%s: %s\n' "$label" "$path"
            if [[ ! -x "$path" ]]; then
              echo "error: $label is missing or not executable" >&2
              return 1
            fi
          }

          require_nonempty() {
            local label="$1" path="$2"
            printf '%s: %s\n' "$label" "$path"
            if [[ ! -s "$path" ]]; then
              echo "error: $label is missing or empty" >&2
              return 1
            fi
          }

          require_contains() {
            local label="$1" actual="$2" expected="$3"
            printf '%s: %s\n' "$label" "$actual"
            if [[ "$actual" != *"$expected"* ]]; then
              echo "error: $label does not contain $expected" >&2
              return 1
            fi
          }

          require_equal() {
            local label="$1" actual="$2" expected="$3"
            printf '%s: actual=%q expected=%q\n' "$label" "$actual" "$expected"
            if [[ "$actual" != "$expected" ]]; then
              echo "error: $label does not match" >&2
              return 1
            fi
          }

          plist_value() {
            /usr/libexec/PlistBuddy -c "Print :$2" "$1"
          }

          require_executable "app executable" "$app/AppbaseIOS"
          require_executable "widget executable" "$widget/AppbaseIOSWidget"
          require_nonempty "App Intents metadata" "$metadata"

          app_architecture="$(file "$app/AppbaseIOS")"
          widget_architecture="$(file "$widget/AppbaseIOSWidget")"
          require_contains "app architecture" "$app_architecture" "arm64"
          require_contains "widget architecture" "$widget_architecture" "arm64"

          require_equal "app bundle identifier" \
            "$(plist_value "$app/Info.plist" CFBundleIdentifier)" \
            "com.example.AppbaseIOS"
          require_equal "widget bundle identifier" \
            "$(plist_value "$widget/Info.plist" CFBundleIdentifier)" \
            "com.example.AppbaseIOS.Widget"
          require_equal "app short version" \
            "$(plist_value "$app/Info.plist" CFBundleShortVersionString)" \
            "0.1.0"
          require_equal "app build version" \
            "$(plist_value "$app/Info.plist" CFBundleVersion)" \
            "1"

          codesign --force --sign - --timestamp=none --generate-entitlement-der \
            --entitlements AppbaseIOSWidget.entitlements "$widget"
          codesign --force --sign - --timestamp=none --generate-entitlement-der \
            --entitlements AppbaseIOS.entitlements "$app"
          codesign --verify --deep --strict "$app"
          app_entitlements="$(codesign --display --entitlements :- "$app" 2>&1)"
          widget_entitlements="$(codesign --display --entitlements :- "$widget" 2>&1)"
          require_contains "app entitlements" "$app_entitlements" \
            "group.com.example.AppbaseIOS.shared"
          require_contains "widget entitlements" "$widget_entitlements" \
            "group.com.example.AppbaseIOS.shared"

          mkdir -p artifacts "$RUNNER_TEMP/AppbaseIOS-ipa/Payload"
          ditto "$app" "$RUNNER_TEMP/AppbaseIOS-ipa/Payload/AppbaseIOS.app"
          (
            cd "$RUNNER_TEMP/AppbaseIOS-ipa"
            ditto -c -k --sequesterRsrc --keepParent Payload "$GITHUB_WORKSPACE/artifacts/AppbaseIOS.ipa"
          )
          unzip -t artifacts/AppbaseIOS.ipa
          shasum --algorithm 256 artifacts/AppbaseIOS.ipa

      - name: Upload IPA
        uses: actions/upload-artifact@v7
        with:
          name: AppbaseIOS-ad-hoc
          path: artifacts/AppbaseIOS.ipa
          if-no-files-found: error
          retention-days: 7
