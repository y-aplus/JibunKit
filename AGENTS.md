# 作業ルール

- 承認済み範囲内の重要でない優先順位や通常の実装順序は自分で判断し、ユーザー判断待ちにしない。ユーザーへの確認は、製品方針・データ互換性・費用・不可逆な影響など、回答によって重要な結果が変わる場合に限る。

- ブランチは無断で切らない。無断で切ったブランチは、ユーザーに無視される。ブランチを切るべき場面でも、事前に明示しないと無視される。
- 当面は、各タスク（計画上のチェックボックスまたは同等の独立作業単位）を完了して必要な検証を通した時点で、そのタスクに属する変更だけをローカルGitコミットとしてチェックポイント化する。チェックポイントごとにはpushせず、外部実行などremoteが必要な節目でまとめて`origin/main`へpushする。このリポジトリではpush前の個別確認を求めない。
- ビルド・CI実行中も、その結果に依存しない実装・レビュー・文書整備を進める。変更単位と検証対象コミットを区別し、共通部分の変更で後続作業の前提が崩れうる場合は結果を待つ。失敗時は影響範囲を確認して関連する修正を優先する。待ち時間を埋めるためだけに機能を追加しない。
- 作業を停止してターンを終える前に、少なくとも1つの実在する待機対象を用意し、何を待ち、結果を受けて何を再開するかを明示する。待機対象は、実行中のCI・外部処理、必要なユーザー判断・承認・実機確認などとする。CIはrun ID、ユーザー判断は実際に提示した質問など、対象を具体的に示す。
- 待つべき対象がない状態では停止せず、承認済みの範囲で次の実装・検証・レビュー・文書整備を進める。結果の記録やコミットだけを理由に停止しない。独立して進められる作業があれば、待機対象があっても続ける。
- 停止条件を満たすためだけの不要な承認要求、同条件のCI再実行、架空の待機対象は作らない。承認済みの作業を完了し、次の作業にユーザーの方針判断が必要なら、具体的な選択肢と判断理由を提示して回答を待つ。

現在の仕様はREADMEから参照する設計文書を優先する。元の検討メモは参考資料であり、そこに書かれた案を現在の確定仕様として扱わない。
