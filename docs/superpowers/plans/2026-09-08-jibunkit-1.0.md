# JibunKit 1.0 完成計画

更新日: 2026-09-08

ユーザーの依頼はJibunKit v1.0完成。個別CI成功やチェックポイントで全体作業を終了しない。設計原則は既存の1.0方向文書を継承し、この計画で実装・検証・出荷の残作業を管理する。通常の技術選択と順序は実装側で判断する。重要な仕様変更・データ互換性の破壊は判断材料を揃えてユーザーへ確認する。

## 完成を判断する条件

| ID | 条件 | 必要な証拠 |
| --- | --- | --- |
| V1 | 独立Swift/SwiftUI Featureを、単独版とJibunKit版で同じ実装から開発・実行できる | 雛形、接続手順、単独起動・ホスト共存のCI。既存コードの分離手順と責任境界 |
| V2 | 複数画面・構造化データ・添付ファイルを持つ実用規模のFeatureで基盤の制限を検証する | 編集・再起動・検索または詳細遷移・他Featureとの独立保存。単純なText雛形だけを証拠にしない |
| V3 | データを保って更新・復旧できる | Featureごとのバックアップ、schema移行、破損拒否、選択外の維持、添付ファイルを含む復旧、失敗状態の説明 |
| V4 | システム連携をFeatureへ接続できる | Widget、App Intents、複数通知、URLの接続手順・CI・出荷候補IPAでの実機確認。OS/署名由来の制約と基盤の未対応を区別 |
| V5 | 作者へ都度相談せず追加・更新・運用できる | WindowsからActionsを使う手順、依存・権限・保存先の分離、基盤更新時の互換性方針、例とテストの対応 |
| V6 | 検証したsourceと配布物が一致する1.0を届ける | 出荷候補の全CI、対象commit、版番号、変更履歴、実機結果、最終公開手順とrelease asset |

自動変換器、動的IPA実行、ストア、クラウド同期、全機種保証は追加しない。Featureの機能・画面数・保存方式を固定しない。検証用アプリが動くことと製品の完成は区別する。

## 実装・検証の順序

### 独立開発と組込み

- [x] Tuist/Swift Packageへ移行し、Core非依存のFeature雛形を提供。
- [x] 生成Exampleの単独起動をCIで確認（34179709036）。
- [x] 同じ生成Featureのホスト内起動・共存・URL遷移を確認（34180768100成功）。
- [x] [Records](../../../Modules/Records/README.md)にFeature/Integration/App Shellへの分離・接続・保存移行の手順と参照実装を揃える。

### 実用データと復旧

- [x] Feature用の独立保存先・ファイル保存API。
- [x] 選択バックアップ・復元画面、失敗報告、実際のFiles往復（34176691075）。
- [x] 構造化データと添付ファイルを持つ参照Featureで、保存・編集・再起動・共存を確認。編集・共存はCI、添付の取込み・プレビュー・保持・削除は2026-09-09中間実機確認（e01836b）で成功。Simulator Quick Lookの制約は検証記録に残す。
- [x] 旧schemaからの移行と、添付ファイルを含む整合したsnapshot/復元を検証（34221667275）。
- [x] ファイル経路と旧JSON互換、128 MiBの別プロセス比較を検証（34225014791）。macOS最大RSSはファイル約23.4 MiB、旧JSON約490.2 MiB。iOS実機上限の保証とはしない。

### システム連携と継続運用

- [x] FeatureへのURL遷移、一覧検索、複数通知IDの基盤。
- [ ] 参照Featureで複数の通知・詳細画面への接続を検証し、不足する接続だけを補う。
- [x] 公開APIの安定範囲・schema/ID/保存先変更の移行方針を[互換性方針](../../compatibility.md)へ文書化。出荷前に実装との一致を再確認する。
- [ ] ビルド・追加・更新・復旧文書の古い記述を検証済みの手順へ統一。
- [x] [Issue #2の構成・CI整理](../../verification/2026-09-08-ci-structure.md): Modulesのテスト実行、app/Widget定義の一本化、通常CIと重い検証の分離。全検証34206702845成功。

### 出荷

- [ ] 1.0候補の全自動検証・差分レビューを実施。
- [ ] そのIPAで上書き、署名更新、保存維持、Widget/Shortcuts/通知、復旧をまとめて実機確認。自動検証可能な残作業を先に済ませる。
- [ ] 版番号・変更履歴・release notes・配布物・検証sourceの対応を確定。
- [ ] 具体的なrelease成果物を準備し、既存公開手順の最終承認を経て公開・配布を確認。

## 保持するもの

既存Counter/ReminderのID・保存値・通知IDとバックアップ形式の読込み互換性を維持する。Zaikoのignoredローカルsource/testsは保持し、製品へ戻さない。既報のupstream issueを重複作成しない。参照Featureは追加開発の実例として扱い、ユーザーの個別アプリを勝手に選定・移植しない。
