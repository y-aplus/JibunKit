# 0.1 検証記録

更新日: 2026-09-01

**状態:** 検証中。F2・F3は合格、F1・F6・F7は調査中、F4・F5は未検証、O1・O2・O3は一部着手、O4は未達。GitHub Actionsで生成した修正版IPAをSideStore 0.6.3からiPhone 16e／iOS 26.6へ導入し、アプリ起動、画面の`1を追加`、Shortcutsの「カウンターに追加」への数値`2`入力、更新値`3`の返却と画面反映、3サイズのWidgetによる共有値表示まで実機で確認した。Widgetの更新はアプリより遅れたが、即時更新を保証しない仕様内である。再起動後の保存、更新インストール、署名更新は未検証。

## 初期環境

| 項目 | 状態 |
| --- | --- |
| Windows | レジストリ表示`Windows 10 Home`／`25H2`／`26200.9168` |
| Git | `2.50.0.windows.1` |
| WSL | `2.7.12.0`、Ubuntu 24.04.4 LTSをWSL 2として導入済み。既定ユーザーは`dev` |
| Swift | Swiftly 1.1.2でSwift 6.3.3を導入済み。Linux向け最小Swiftパッケージの生成・ビルド・実行に成功 |
| xtool | 1.17.0を導入済み。公式release digest一致、基本ヘルプとビルドサブコマンドのヘルプ起動に成功 |
| Apple SDK | Xcode 26.6 Universal XIPの実ファイルをsize・magic・SHA-1で確認し、Apple Developer ServicesへログインせずDarwin Swift SDKを生成。iPhoneOS 26.5、iPhoneSimulator 26.5、macOS 26.5 SDKを導入済み |
| 最小iOSビルド | `xtool new --skip-setup`で生成したSwiftUI例に対し、`xtool dev build`と`xtool dev build --ipa`が終了コード0。arm64 Mach-Oを含む未署名の`.app`とIPAをリポジトリ外へ保存 |
| 製品最小構成 | WSLではApp Intent型、本体、Widget extensionを含む0.1.0 IPAを生成したがApp Intentsメタデータは欠落。Actionsでは公式メタデータを含むIPAの検査・取得まで成功 |
| GitHub CLI | 公式portable版`2.98.0`をWindowsへ導入し、`y-aplus`としてworkflow起動・監視・ログ取得・artifact取得に成功。認証tokenはリポジトリへ保存していない |
| 実機 | iPhone 16e／iOS 26.6／SideStore 0.6.3。IPA導入、アプリ起動、画面更新、Shortcuts登録・入出力、3サイズのWidget表示を実施済み |

詳細と確認コマンドは[ビルド経路の判断記録](../decisions/build-route.md)を参照する。

## 完成条件の状態

| ID | 状態 | 証拠・備考 |
| --- | --- | --- |
| F1 | 調査中 | SideStore導入後の起動と画面の`1を追加`による値`1`への更新は成功。アプリ終了・再起動後の保存維持は未検証 |
| F2 | 合格 | 修正コミット`ff0ca65`、Actions run `33460927742`のIPAを使用。「カウンターに追加」が実機のShortcutsへ現れ、画面で`1`にした後に数値`2`を渡すと更新値`3`を返し、アプリ画面も`3`を表示した。画面とIntentは同じ`CounterStore.add(_:)`を使用 |
| F3 | 合格 | SideStore再署名後、3サイズのWidgetが表示され、アプリとShortcutsで更新した共有値を表示した。アプリより遅れて反映されたが、実装は15分後を次回更新候補にする表示専用Timelineであり、仕様も即時更新を保証しない |
| F4 | 未着手 | ローカル通知は未検証 |
| F5 | 未着手 | 第2ミニアプリ追加は未検証 |
| F6 | 調査中 | 無料AppleアカウントとSideStore 0.6.3による初回導入・起動は成功。更新インストール、署名更新、各機能と保存値の維持は未検証 |
| F7 | 調査中 | WSL 2のローカル経路はApp Intentsメタデータ不足で不成立。`macos-26`／Xcode 26.6の修正版run `33460927742`は1分38秒で成功し、79,730-byte、SHA-256 `a52c1a2b55fd18bd4ad596d26a4e5dcf6bcc2b66869bba63a22dad58a28cd485`のIPAを取得・再検査した。SideStore導入とF2・F3は合格したがF1・F4〜F6が未完了のため確定扱いにはしない |
| O1 | 一部着手 | Git管理と非公開保存を開始済み。公開を含む条件は未達 |
| O2 | 一部着手 | 設計・計画文書は存在するが、実証済みの導入・追加・更新手順は未作成 |
| O3 | 一部着手 | `CounterFeature`の単体テスト5件をLinuxとActionsで実行し、手動起動workflow内のビルド・IPA検査も合格。F2・F3の合格とF7の途中結果を記録したが、残りの実機結果は未整備 |
| O4 | 未着手 | 0.1リリース成果物は存在しない |

自動チェック、資料確認、ビルド成功、SideStore導入、実機動作をそれぞれ別の証拠として記録し、未実施項目を合格扱いにしない。
