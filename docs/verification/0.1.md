# 0.1 検証記録

更新日: 2026-08-31

**状態:** 検証中。F1・F4〜F6は未検証、F2・F3・F7は調査中、O1・O2・O3は一部着手、O4は未達。製品へ継続利用する最小のApp Intent／Widget構成を実装した。WSLではApp Intentsメタデータを生成できずローカル経路を不成立と判定したが、GitHub Actions run `33395722076`ではXcode 26.6が生成した公式メタデータ、arm64本体、Widgetを含むアドホック署名IPAの検査とartifact uploadまで成功した。SideStore導入と実機動作は未検証。

## 初期環境

| 項目 | 状態 |
| --- | --- |
| Windows | レジストリ表示`Windows 10 Home`／`25H2`／`26200.9168` |
| Git | `2.50.0.windows.1` |
| WSL | `2.7.12.0`、Ubuntu 24.04.4 LTSをWSL 2として導入済み。既定ユーザーは`dev` |
| Swift | Swiftly 1.1.2でSwift 6.3.3を導入済み。Linux向け最小Swiftパッケージの生成・ビルド・実行に成功 |
| xtool | 1.17.0を導入済み。公式release digest一致、基本ヘルプとビルドサブコマンドのヘルプ起動に成功 |
| Apple SDK | Xcode 26.6 Universal XIPの実ファイルをsize・magic・SHA-1で確認し、Apple Developer ServicesへログインせずDarwin Swift SDKを生成。iPhoneOS 26.5、iPhoneSimulator 26.5、macOS 26.5 SDKを導入済み |
| 最小iOSビルド | `xtool new --skip-setup`で生成したSwiftUI例に対し、`xtool dev build`と`xtool dev build --ipa`が終了コード0。arm64 Mach-Oを含む未署名の`.app`とIPAをリポジトリ外へ保存 |
| 製品最小構成 | WSLではApp Intent型、本体、Widget extensionを含む0.1.0 IPAを生成したがApp Intentsメタデータは欠落。Actionsでは公式メタデータを含むIPAの検査・取得まで成功 |
| GitHub CLI | 公式portable版`2.98.0`をWindowsへ導入し、`y-aplus`としてworkflow起動・監視・ログ取得・artifact取得に成功。認証tokenはリポジトリへ保存していない |
| 実機 | iPhone 16e／iOS 26.6／SideStore 0.6.3という申告を検証対象にしている。実機の表示確認は未実施 |

詳細と確認コマンドは[ビルド経路の判断記録](../decisions/build-route.md)を参照する。

## 完成条件の状態

| ID | 状態 | 証拠・備考 |
| --- | --- | --- |
| F1 | 未着手 | 起動・保存は未検証 |
| F2 | 調査中 | `AddCounterValueIntent`と`AppbaseShortcuts`はリンク済み。Actions run `33395722076`でXcode標準processorが生成した2,494-byteの`Metadata.appintents/extract.actionsdata`を含むIPAを確認。Shortcuts登録・数値入力・戻り値は未検証 |
| F3 | 調査中 | Actions IPAの`AppbaseIOSWidget-Extension.appex`と、本体・Widget双方の論理App Group文字列を確認。SideStore再署名後の共有領域解決と実値表示は未検証 |
| F4 | 未着手 | ローカル通知は未検証 |
| F5 | 未着手 | 第2ミニアプリ追加は未検証 |
| F6 | 未着手 | SideStore導入・更新は未検証 |
| F7 | 調査中 | WSL 2のローカル経路はApp Intentsメタデータ不足で不成立。`macos-26`／Xcode 26.6のrun `33395722076`はjob 1分19秒で成功し、79,561-byte、SHA-256 `5a4ec66c6c561722b6788f012d1f6949ccdf143493873f8bd2e629ea1eeef3d3`のIPAを取得・再検査した。SideStore導入が未検証のため確定扱いにはしない |
| O1 | 一部着手 | Git管理と非公開保存を開始済み。公開を含む条件は未達 |
| O2 | 一部着手 | 設計・計画文書は存在するが、実証済みの導入・追加・更新手順は未作成 |
| O3 | 一部着手 | `CounterFeature`の単体テスト5件をLinuxとActionsで実行し、手動起動workflow内のビルド・IPA検査も合格。F2・F3・F7の途中結果を記録したが、残りの実機結果は未整備 |
| O4 | 未着手 | 0.1リリース成果物は存在しない |

自動チェック、資料確認、ビルド成功、SideStore導入、実機動作をそれぞれ別の証拠として記録し、未実施項目を合格扱いにしない。
