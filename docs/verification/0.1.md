# 0.1 検証記録

更新日: 2026-09-04

**状態:** JibunKitへの改名後を再検証中。本体`com.jibunkit.app`、Widget `com.jibunkit.app.Widget`、App Group `group.com.jibunkit.shared`へ変更し、WSLの全11テスト、ローカルiOSビルド、`JibunKit.ipa`のZIP構造とbundle ID検査は合格した。Shortcuts用の公式App Intentsメタデータを含むActionsビルドと、SideStoreでの初回導入・更新・署名更新は未実施である。

旧称・旧識別子ではiPhone 16e／iOS 26.6／SideStore 0.6.3上でF1〜F7に合格した。ただしbundle IDとApp Groupを変更したJibunKitは別アプリであり、その実機結果を現在の版の合格として流用しない。旧アプリの保存値を移行する仕様も設けない。

## 現在の環境

| 項目 | 状態 |
| --- | --- |
| Windows | レジストリ表示`Windows 10 Home`／`25H2`／`26200.9168` |
| Git | `2.50.0.windows.1` |
| WSL | `2.7.12.0`、Ubuntu 24.04.4 LTSをWSL 2として導入済み。既定ユーザーは`dev` |
| Swift | Swiftly 1.1.2、Swift 6.3.3 |
| xtool | 1.17.0。公式release archiveのdigest確認済み |
| Apple SDK | Xcode 26.6 Universal由来。Apple Developer ServicesへログインせずDarwin Swift SDKを生成済み |
| GitHub CLI | 公式portable版`2.98.0`。認証tokenはリポジトリへ保存していない |
| 実機 | 再確認対象はiPhone 16e／iOS 26.6／SideStore 0.6.3 |

詳細と過去の調査結果は[ビルド経路の判断記録](../decisions/build-route.md)を参照する。

## 完成条件の状態

| ID | 状態 | 証拠・次の確認 |
| --- | --- | --- |
| F1 | 再確認待ち | JibunKitを初回導入し、画面更新、完全終了後の再読込み、別JibunKitビルドへの上書き後の保持を確認する |
| F2 | 再確認待ち | Actions IPAで「カウンターに追加」の登録、数値入力、戻り値、画面反映を確認する |
| F3 | 再確認待ち | 3サイズのWidgetが共有値を表示し、上書き・署名更新後も動くことを確認する。即時反映は保証しない |
| F4 | 再確認待ち | 起動時に許可を要求しないこと、拒否後の通常操作、foregroundと終了状態からの通知タップを確認する |
| F5 | 再確認待ち | カウンターとリマインダーの一覧・独立保存・遷移を確認する。IDと保存namespaceの自動テストは合格済み |
| F6 | 再確認待ち | 同じJibunKit識別子の別ビルドへ上書きし、F1〜F5を維持することと、SideStore署名更新後の継続動作を確認する |
| F7 | 一部合格 | WSLで全11テストとJibunKit名のIPA生成・構造検査に合格。改名後の`macos-26`／Xcode 26.6 Actions runと、そのIPAによるF1〜F6が残る |
| O1 | 公開待ち | source、MIT License、第三者noticeは存在する。GitHub repositoryのJibunKitへの改名と、最終承認後のpublic切替が残る |
| O2 | 合格 | README、設計、ビルド、SideStore、追加、更新、貢献、Security、変更履歴、公開手順をJibunKitの名称・識別子・再検証状態へ更新し、ローカルlink検査に合格した |
| O3 | 再確認中 | 全11ローカルテストとpublication boundaryは合格。改名後のActionsと実機結果を別証拠として追加する |
| O4 | 未着手 | 0.1リリース成果物は存在しない |

自動チェック、資料確認、ビルド成功、SideStore導入、実機動作を別々の証拠として扱い、未実施項目を合格扱いにしない。

## 改名前の実機履歴

旧称・旧識別子のActions IPAでは、次を確認した。この履歴は機能とビルド経路の先行証拠であり、JibunKit識別子の最終承認には使わない。

- アプリ再起動後の保存値、画面とShortcutsからの加算、3サイズのWidget表示。
- カウンターとリマインダーの独立保存、通知許可の拒否と許可、foregroundと終了状態からの通知タップ。
- 同一識別子での上書き更新とSideStore署名更新後の保存値・Shortcuts・Widget・通知。
- クリーンなGitHub-hosted macOS runnerでのXcode 26.6ビルド、公式App Intentsメタデータ、Widget、署名構造、IPA整合性。

## 改名後の公開前チェック

- [x] Swift package、source、plist、entitlements、workflowから旧製品名と旧識別子を除去した。
- [x] WSLで全11テストを実行した。
- [x] WSLで`JibunKit.ipa`を生成し、本体・Widgetの名称、bundle ID、ZIP整合性を検査した。
- [x] workflow YAMLとpublication boundaryをローカル検査した。
- [ ] GitHub repositoryをJibunKitへ改名し、private、default branch `main`、MIT Licenseを確認する。
- [ ] 改名後のrelease対象commitをActionsで処理し、全stepとartifactを検査する。
- [ ] JibunKitをSideStoreで初回導入し、F1〜F5を確認する。
- [ ] 別のJibunKitビルドへ上書きし、F1〜F6を確認する。
- [ ] SideStoreで署名更新し、保存値と各連携を確認する。
- [ ] 最終IPAにcredential、個人の署名材料、provisioning、pairing、SDK、実データがないことを確認する。
- [ ] 明示的な最終承認後にtag、public切替、releaseを行い、O1・O4を確認する。
