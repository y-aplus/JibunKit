# 0.1 検証記録

更新日: 2026-09-04

**状態:** 公開準備中。F1〜F7、O2、O3は合格した。O1は公開前のGit・ライセンス・private GitHub確認まで合格し、public切替だけを工程6に残す。O4は未達。Actions run `33465093595`のIPAを既存アプリへ上書き更新し、リマインダーの保存、通知許可の拒否と許可、foregroundと終了状態からの通知タップ、カウンター・Shortcuts・Widgetの継続動作をiPhone 16e／iOS 26.6／SideStore 0.6.3で確認した。その後の署名更新でも保存値と各連携を維持した。Mac購入不要の確定ビルド・導入経路は`docs/build.md`と`docs/sidestore.md`に記録した。

## 初期環境

| 項目 | 状態 |
| --- | --- |
| Windows | レジストリ表示`Windows 10 Home`／`25H2`／`26200.9168` |
| Git | `2.50.0.windows.1` |
| WSL | `2.7.12.0`、Ubuntu 24.04.4 LTSをWSL 2として導入済み。既定ユーザーは`dev` |
| Swift | Swiftly 1.1.2でSwift 6.3.3を導入済み。Linux向け最小Swiftパッケージの生成・ビルド・実行に成功 |
| xtool | 1.17.0を導入済み。公式release digest一致、基本ヘルプとビルドサブコマンドのヘルプ起動に成功 |
| Apple SDK | Xcode 26.6 Universal XIPの実ファイルをsize・magic・SHA-1で確認し、Apple Developer ServicesへログインせずDarwin Swift SDKを生成。iPhoneOS 26.5、iPhoneSimulator 26.5、macOS 26.5 SDKを導入済み |
| 最小iOSビルド | `xtool new --skip-setup`で生成したSwiftUI例に対し、`xtool dev build`と`xtool dev build --ipa`が終了コード0。arm64 Mach-Oを含む未署名の`.app`とIPAをリポジトリ外へ保存 |
| 製品最小構成 | WSLではApp Intent型、本体、Widget extensionを含む0.1.0 IPAを生成したがApp Intentsメタデータは欠落。Actionsでは公式メタデータを含むIPAの検査・取得まで成功 |
| GitHub CLI | 公式portable版`2.98.0`をWindowsへ導入し、`y-aplus`としてworkflow起動・監視・ログ取得・artifact取得に成功。認証tokenはリポジトリへ保存していない |
| 実機 | iPhone 16e／iOS 26.6／SideStore 0.6.3。更新インストール、両ミニアプリの独立保存、通知許可の拒否と許可、foregroundと終了状態からの通知タップ、Shortcuts登録・入出力、3サイズのWidget表示を実施済み |

詳細と確認コマンドは[ビルド経路の判断記録](../decisions/build-route.md)を参照する。

## 完成条件の状態

| ID | 状態 | 証拠・備考 |
| --- | --- | --- |
| F1 | 合格 | 初回IPAで値`3`の状態からアプリを完全終了して開き直しても`3`を再読込みした。工程2のrun `33462672016`へ上書き更新後も`3`が残り、ミニアプリ一覧の「カウンター」から画面を開き、`1を追加`で`4`へ更新した |
| F2 | 合格 | run `33460927742`で「カウンターに追加」がShortcutsへ現れ、数値入力、更新値の返却、画面反映を確認した。工程2のrun `33462672016`へ上書き更新後も継続動作した。画面とIntentは同じ`CounterStore.add(_:)`を使用 |
| F3 | 合格 | SideStore再署名後、3サイズのWidgetが共有値を表示した。工程2のrun `33462672016`へ上書き更新後も継続動作した。アプリより遅れて反映されるが、15分後を次回更新候補にする表示専用Timelineであり、仕様も即時更新を保証しない |
| F4 | 合格 | 起動だけでは許可画面が出ず、予約操作での拒否後も保存とカウンターを使用できた。許可後、foregroundと終了状態の双方で通知を受け、タップからリマインダーへ戻れた |
| F5 | 合格 | 一覧にカウンターとリマインダーを表示し、リマインダーの保存・再読込みがカウンター値を変えないことを確認した。ID・保存namespace・通知IDの衝突検査も合格 |
| F6 | 合格 | 工程2から工程3のIPAへ同じ端末・アカウント・アプリ識別子で上書き更新し、F1〜F5の保存値と各連携を維持した。LocalDevVPN接続下でSideStoreの残り日数をタップして署名更新した後も、両ミニアプリの保存値、Widget、Shortcuts、終了状態の通知タップからの起動を確認した |
| F7 | 合格 | WSL 2のローカル経路はApp Intentsメタデータ不足で不成立。新しい`macos-26` runnerでcommit `a3098ef`を処理したXcode 26.6 run `33465093595`は1分6秒で成功し、全10テスト、公式メタデータ、本体、リマインダー通知、Widgetを含むIPAを生成・検査した。同一IPAのSideStore上書き・署名更新後にF1〜F6を確認した。Macの購入、Apple認証情報のActions投入、生成メタデータの手修正は不要 |
| O1 | 公開待ち | Git管理されたsource・sample・検証・文書、private GitHubの`main`、MIT License、第三者noticeを確認済み。public repositoryの確認だけを0.1 releaseと同時に行う |
| O2 | 合格 | README、build、SideStore導入、ミニアプリ追加、基盤更新、貢献、通常問題報告、脆弱性報告、変更履歴、公開・releaseの実行手順が現在の構成と確認済み環境に一致する |
| O3 | 合格 | WSLの全11テスト、実機F1〜F7、基盤更新時の競合確認に加え、固定されたActions依存とpublication boundaryを含むrun `33825680660`が合格した。自動checkと実機結果を別に記録している |
| O4 | 未着手 | 0.1リリース成果物は存在しない |

自動チェック、資料確認、ビルド成功、SideStore導入、実機動作をそれぞれ別の証拠として記録し、未実施項目を合格扱いにしない。

## 公開前チェック

- `y-aplus/AppbaseIOS`がprivate、default branchが`main`、license表示がMITであることをGitHub APIとCLIで確認した。
- README、LICENSE、第三者notice、貢献、Security Policy、変更履歴、build・導入・追加・更新・公開手順がprivateの`main`から取得できることを確認した。
- 追跡済み42fileのsecurity reviewでは、credential、個人の署名材料、provisioning profile、pairing file、Team ID、固有端末ID、Apple SDK、IPA、実データの混入は見つからなかった。外部GitHub Actionの可変参照1件を修正した。
- [Actions run `33825680660`](https://github.com/y-aplus/AppbaseIOS/actions/runs/33825680660)で、固定action、publication boundary、全10テスト、Xcode build、App Intents metadata、本体・Widget、署名構造、IPA整合性、artifact uploadの全stepが合格した。
- 同runから取得したIPAは16項目で構成され、禁止対象の鍵・証明書・provisioning・pairing・SDK fileと代表的なcredential形式を含まないことを確認した。これは公開releaseではなく、工程6ではrelease対象commitから再生成する。
