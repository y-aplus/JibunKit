# 0.1 検証記録

更新日: 2026-08-30

**状態:** 検証中。F1・F4〜F6は未検証、F2・F3・F7は調査中、O1・O2・O3は一部着手、O4は未達。製品へ継続利用する最小のApp Intent／Widget構成を実装し、WSLでテストとIPA生成に成功した。WidgetはIPAに入ったがApp Intentsメタデータがないため、F2のローカル経路は不成立と判定した。GitHub Actionsの初回runではXcodeによるメタデータ生成まで成功したが、アプリwrapperへのコピーが欠けてIPA検査に失敗した。

## 初期環境

| 項目 | 状態 |
| --- | --- |
| Windows | レジストリ表示`Windows 10 Home`／`25H2`／`26200.9168` |
| Git | `2.50.0.windows.1` |
| WSL | `2.7.12.0`、Ubuntu 24.04.4 LTSをWSL 2として導入済み。既定ユーザーは`dev` |
| Swift | Swiftly 1.1.2でSwift 6.3.3を導入済み。Linux向け最小Swiftパッケージの生成・ビルド・実行に成功 |
| xtool | 1.17.0を導入済み。公式release digest一致、基本ヘルプとビルドサブコマンドのヘルプ起動に成功 |
| Apple SDK | Xcode 26.6 Universal XIPの実ファイルをsize・magic・SHA-1で確認し、Apple Developer ServicesへログインせずDarwin Swift SDKを生成。iPhoneOS 26.5、iPhoneSimulator 26.5、macOS 26.5 SDKを導入済み |
| 最小iOSビルド | `xtool new --skip-setup`で生成したSwiftUI例に対し、`xtool dev build`と`xtool dev build --ipa`が終了コード0。arm64 Mach-Oを含む未署名の`.app`とIPAをリポジトリ外へ保存 |
| 製品最小構成 | App Intent型、本体、Widget extensionを含む0.1.0 IPAを生成。`swift test`は5件合格、IPAのZIP整合性とarm64本体・Widgetを確認。App Intentsメタデータは欠落 |
| GitHub CLI | 未導入 |
| 実機 | iPhone 16e／iOS 26.6／SideStore 0.6.3という申告を検証対象にしている。実機の表示確認は未実施 |

詳細と確認コマンドは[ビルド経路の判断記録](../decisions/build-route.md)を参照する。

## 完成条件の状態

| ID | 状態 | 証拠・備考 |
| --- | --- | --- |
| F1 | 未着手 | 起動・保存は未検証 |
| F2 | 調査中 | `AddCounterValueIntent`と`AppbaseShortcuts`はクロスコンパイル・リンク済み。Actions run `33392043166`でXcode標準processorが`Metadata.appintents`を生成したが、xtool生成wrapperが`.app`へコピーせずIPAは未生成。梱包修正とShortcuts登録・数値入力・戻り値は未検証 |
| F3 | 調査中 | `AppbaseIOSWidget.appex`を含むIPA生成と本体・Widget双方の論理App Group文字列を確認。SideStore再署名後の共有領域解決と実値表示は未検証 |
| F4 | 未着手 | ローカル通知は未検証 |
| F5 | 未着手 | 第2ミニアプリ追加は未検証 |
| F6 | 未着手 | SideStore導入・更新は未検証 |
| F7 | 調査中 | WSL 2のローカル経路はApp Intentsメタデータ不足で不成立。`macos-26`／Xcode 26.6の初回runは1分42秒で、Xcode生成まで成功したがwrapperへのコピー不足で失敗。公式生成物を内容変更せず梱包する修正を次に再検証する |
| O1 | 一部着手 | Git管理と非公開保存を開始済み。公開を含む条件は未達 |
| O2 | 一部着手 | 設計・計画文書は存在するが、実証済みの導入・追加・更新手順は未作成 |
| O3 | 一部着手 | `CounterFeature`のLinux単体テスト5件を実行し、F2・F3・F7の途中結果を記録した。自動チェック設定と残りの実機結果は未整備 |
| O4 | 未着手 | 0.1リリース成果物は存在しない |

自動チェック、資料確認、ビルド成功、SideStore導入、実機動作をそれぞれ別の証拠として記録し、未実施項目を合格扱いにしない。
