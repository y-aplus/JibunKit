# 0.4.0公開記録

状態: 候補検証中、未公開。

ユーザーの中間版公開の依頼に従い、検証済みのmain `f89997d`を0.4.0 build 5候補とする。候補差分は版番号、CIの版検査とshell字下げの整理、公開文書。未統合のKeychain access-control拡張、privacy manifest検証fixture、ignored Zaikoは含めない。

検証計画: 出荷sourceで共有unit・通常app/Widget build・IPA構造/版/署名/言語resource整合性を確認し、通常Simulator suiteとFiles backup round tripを実行する。Feature専用の検証済み範囲は下記source/CIを参照する。候補で実行しない試験を全回帰成功に含めない。

- 通知request配送: `c11bcaf` / 34528052611。共有155試験、実foreground二ownerとcustom action配送133.529秒、他Feature表示維持。
- 多言語設定: `9f67762` / 34528323199。native fixture app/Widgetのen/ja、削除済みlocale除外、通常bundleと生成設定の一致。
- Spotlight配送: `eba6fc1` / 34523416261。OS検索結果からの詳細、別Feature経路保持、終了後の起動269.418秒。
- UIKit background所有権: `5f4aa2e` / 34519837163。実token取得/終了18.539秒。期限切れは注入provider試験であり実OS期限切れではない。
- その他の累積変更はCHANGELOGと差分台帳の個別記録に対応する。

公開時はCI由来IPAを直接assetとして配布し、全entryの展開/CRC、版情報、SHA-256と公開直リンク再取得の一致を記録する。候補sourceとtagの対応を固定し、過去版のassetは差し替えない。
