# P0-A 寿命・保存・終了診断

状態: 実装を統合し、初回CI前の一括レビューを完了。Swift/iOSの実行結果は未確認。
方針baseline: `2c04cd3`。[契約と分担](../delivery/P0-A-contract.md) / [版の境界](../implementation-priorities.md)。

## 実装と接続

- `MiniAppFeatureLifetime`で開始/終了/失敗/再試行、同時caller、構成失敗のcleanup、復元前の稼働状態に応じた再開を管理。
- Definitionの任意lifetimeと通常rootの準備/失敗/再試行、BackupScreenのeffectiveRestoreLifecycleを接続。Counter/Reminder/Recordsで利用。
- Runtime終了進捗は既存Task集合から直接読み、Task待ち/cleanup待ち/完了と残数・開始時刻を区別。待機用TaskGroupや周期pollは追加しない。
- `withStoreMaintenance`で移行/リセットを既存owner予約とstop/apply/resumeへ接続。失敗時もresumeし、Valueを返す。
- RecordsはCore非依存の注入境界で通常保存/添付を予約へ参加。snapshotの二重予約を避け、移行はversion2でも破損を検証、resetは所有ディレクトリのみ置換。
- SQLiteはnative transaction・rollback・close/reopenとB保持を比較。iOS fixtureでもactorが同期SQLを所有し、閉鎖確認を省略しない。

## 合格条件と試験の対応

| 条件 | 実装/試験 | 初回CI前の状態 |
| --- | --- | --- |
| P0-1.lifetime | MiniAppFeatureLifetimeTests、既存Runtime/NotificationObservations/URLSessionLifetime/SQLite tests | Swift未実行 |
| P0-1.integration | P0ALifetimeUITests 2件。通常Definitionから開始、非選択で継続、A終了/起動失敗/再開時BのTask値と購読維持 | iOS未実行 |
| P0-3.failure | RestoreCoordinator/Lifecycle、Records store/backup、native SQLiteの破損・取消・拒否・rollback・B保持 | Swift未実行 |
| P0-3.integration | P0ARecordsUITests、P0ASQLiteUITests、既存Records通常UI | iOS未実行 |
| P0-3.device | 0.7.0候補で選択復元/リセット/他Feature保持をまとめる | 実機待ちを0.7.0へ予約 |
| P0-6.diagnostics | MiniAppRuntimeShutdownProgressTests、既存Runtime/TaskScope tests | Swift未実行 |
| P0-6.local | runtime-shutdown-progress guideの短い局所値変換と主スレッド契約を検討。外部資源/global登録を伴う処理と区別 | ソース/文章レビュー済み |

## 初回CI計画（同一の固定SHAで2 run）

1. 通常構成: `simulator_tests=true`、`feature_validation=false`、filter空。共有/Module tests、通常IPA/metadata、通常UI回帰を実施。
2. 生成/比較構成: `simulator_tests=true`、`feature_validation=true`、`ui_test_filter=MigrationUITests/MigrationUITests/testMiniAppLinksOpenColdAndSwitchWarm`。
   `feature_ui_test_filter`は下記4指定のカンマ区切り。他のbool入力はfalse、runtime指定は空（既定のiOS 26）。

```
MigrationUITests/P0ALifetimeUITests
MigrationUITests/P0ARecordsUITests
MigrationUITests/P0ASQLiteUITests
MigrationUITests/GeneratedFeatureUITests/testRecordsUsesIndependentHostStorage
```

生成側はNotes単独/hostとRecords単独buildも実施。通常run全回帰と生成runの限定試験を混同しない。
見込みは通常25分/生成30分、各job既存timeout45分を維持。実績を見て分割を修正する。
CI前のreportは`.git/P0-A-preflight.json`でsourceと正確な入力を固定し、`Tools/check-delivery.py --stage preflight`で検査する。
成果が揃ってから起動し、完了はOS監視と一回のqueue通知へ任せる。

## 一括レビューと残る確認

保存担当は初回提出9fb56f0に一括指摘し621d123で修正。async assertion、version2破損、transaction rollbackと取消/拒否時保持を追加。
同担当のSQLite iOS比較14b9295も統合し、親がnative close成功確認を補った。
診断担当は1ea4b61に一括指摘し4f39b42で修正。非公開memberwise initのtest利用と追加待機Task群を除去。
親は共通起動契約、Records通常host接続、iOSの寿命/JSON添付比較、workflow membership/filter環境変数をまとめて確認。

ローカルPython工具18試験・Swift UI test selector・workflow埋込みPython構文・diff whitespaceを検査。
WindowsにはSwift/Xcodeがないため、コンパイル成功や実OS挙動はまだ主張しない。通常IPAへ診断fixtureを混入させない。
P0-1/3/6や親Dのcomplete判定、0.7.0出荷はまだ行わない。

## 運用実績の記録欄

担当ごとの小commitでCIを起動した回数: 0。初回CIは2 runを予定。
担当との一括指摘/修正は保存・診断各1往復。保存のSQLite iOS追加は、親が予定していた残検証の分担変更。
診断担当の初回turnが本文なしで終わったため、一度再開確認を送った。
実際のrun/attempt数、job分数、親管理量、費用は完了後に記録する。現時点では節約率を算出しない。
0.7.0完了時に別途調査結果が未反映ならユーザーへ要求する。
