# P0-B 画面・同意・無効化/削除

2026-09-12。初回CIの共通239試験（skip2）とRecords11試験は成功。通常CI34695873582成功後、生成比較34697206948の提示/管理UIで失敗。ホストの提示元保持を修正して両構成を再検証する。P0-Aの通常/生成成功証拠と修正を取り込んだ。

## 現在の実装

- Feature別同意宣言/保存とCounter/Reminder削除providerを別担当から取り込んだ。データ形式/キーを変更せず所有キーだけ削除する。
- 共通管理層は最初のawait前に起動受付を閉じ、無効化/削除の意図をowner別に永続化。保存領域の排他予約、Runtime終了、所有登録解除、削除、同意消去の順で処理する。
- 無効化はデータ/同意を保持する。削除は再登録まで起動を拒否し、再登録で以前の同意を復活させない。解除/削除は再試行可能であることをcallback契約とする。
- エラーでは途中状態と失敗段階を残す。再起動でも途中状態を有効に戻さない。終了が進まなければ待機処理/解放件数を管理画面で表示する。
- 通常hostの一覧、経路解決、通知、scene/hostイベントに受付条件を接続。cold launchのネイティブ登録自体は必要時刻に行い、そこからの業務処理はFeature lifetime経由の契約を維持する。
- アプリ内管理画面に無効化、再有効化、対象/データ説明と確認付き削除、再試行、利用同意変更を追加。リマインダーの通知操作に目的/拒否時動作の説明と同意判定を接続。
- Counter更新、Reminder保存/通知の通常ボタン処理をRuntime所有にして、削除前の終了待ちへ含める。

## 検証と残件

Core tests追加: Aの無効化でデータ保持、削除と再登録で同意初期化/B保持、削除失敗後の再起動と再試行、保存予約競合時の保持。
保存・snapshot・restoreの共通予約も、無効/削除中/削除済みownerの新規受付を拒否する。管理の後始末だけが専用の内部予約で進める。
この受付は管理層初期化中に同期設定し、保存済みの途中状態をcold launchのイベントに先行して適用する。
Core testsに、削除後の通常アクセス/保守拒否、再登録後の受付回復、破損した管理状態の起動拒否と他owner保持を追加。
管理層の終了待ち結合testも追加した。Aの所有taskが取消を受けても実際には戻らない間、状態はremoving/stoppingで、登録解除もデータ削除も未実行であること、Bの通常保存アクセスが継続することを確認する。明示gate解放後にだけ解除/削除が完了する。
通常UI testはCounterの更新→無効化→再起動→URL拒否→再有効化→削除取消→値保持→削除→再登録で0→Reminder保持を通す。
既存の通知UI testにはFeature同意の説明画面を追加した。
これらは未実行。WindowsのPythonツール検証はSwiftやUIKitの証拠に代えない。

P0-B初回CIまでに、管理/拒否/取消の通常UIテスト、提示中の外部遷移、UIKit終了、画面保持/再生成/reset契約を一式にする。
提示担当の4da3678/5eb215eを一括レビュー後に統合。Runtime世代付きhandle、同時終了の共有、closed Runtimeの提示拒否、旧世代cleanupから新世代の保護を実装した。
親はdidEndでUI handleが消えた後も非同期dismiss callbackが実行中なら終了待ちに含める条件を追加した。生成hostのP0BPresentationUITestsでSwiftUI提示/取消、提示中URL遷移、UIKit内からの外側stop→dismiss完了→再開、B保持を検査する。
通常管理UI testsにFeature同意の拒否→通知再操作の拒否→通常メッセージ編集/保存の維持→同意変更を追加。これらのSwift/UI試験は未実行。
別sceneで既に開いたbackupは同じcoordinatorの予約時に拒否する。別processやOS外部入口（Widget/Intentを含む）からの受付との整合は引き続きレビューし、未接続入口を有効化済みと扱わない。
通常Counter Shortcutはcold launchでも管理状態を初期化して有効性を確認し、更新を同じowner保存予約へ接続した。無効化/削除後の更新は拒否する。実OS Shortcutでの拒否・再有効化後の回復は候補検証に含める。Widgetの表示更新と共有状態はまだ確認が残る。
管理状態の保存先を、通常hostでは既存App Group解決による共有defaultsへ接続。Widgetは同じ状態を読み、無効/削除済みなら保存値を表示せず、状態変更時にhostからtimeline更新を要求する。共有設定が解決できないhostではlocal管理を維持し、Widget側は共有領域が読めない表示となる。
独立したdefaults readerでAだけの状態変更を読むunit testを追加した。これは別processのOS更新時刻の証拠ではなく、Widgetの反映時刻はOS調停に依存する。既存0.6.0には管理状態キーがなく、Feature保存/backup形式は変更していない。候補のWidget/Shortcut確認は0.7.0へまとめる。
所有Runtimeを経由しない任意Featureコードの強制停止や、同一processのセキュリティ隔離は提供しない。
通常UIと生成比較を統合レビューしてから、P0-Bの大きなCI境界で実行する。小変更ごとのCIは起動していない。

0.7.0候補時に実機操作をまとめ、別途進行中の需要調査が未反映ならユーザーに提供を依頼する。

## P0-Bを凍結する前の検証計画

P0-Aは生成比較34694032847成功、非実機条件の証拠を確定済み。P0-Bはその結果と修正を取り込み、通常構成から初回CIを開始する。担当の提出単位ではCIを起動しない。

| run | 操作と範囲 | 見込み / timeout |
| --- | --- | --- |
| 通常 | simulator_tests=true、feature_validation=false、filter空。全Core/Module tests、通常app/widget・IPA・standalone Counter、既存通常UI/Files往復、MiniAppManagementUITestsの拒否/保持/取消/削除/再登録 | 30分 / 45分 |
| 生成比較 | simulator_tests=true、feature_validation=true。通常ui_test_filterはtestMiniAppLinksOpenColdAndSwitchWarm。生成側は下記のまとめ指定、Notes/Records単独buildと既存独立保存も含む | 30分 / 45分 |

生成feature_ui_test_filter予定（同一修正候補のSHAを固定）:

```
MigrationUITests/P0ALifetimeUITests,
MigrationUITests/P0ARecordsUITests,
MigrationUITests/P0ASQLiteUITests,
MigrationUITests/P0BPresentationUITests,
MigrationUITests/P0BManagementFailureUITests,
MigrationUITests/GeneratedFeatureUITests/testRecordsUsesIndependentHostStorage
```

入力時は改行を除いたカンマ区切りにする。P0BManagementFailureUITestsは17afe4eをレビューして統合し、通常管理画面の登録解除失敗→未完了表示→再試行→完了→再登録とB保持を確認する。親は初期保存値の読込待ちを明確化し、失敗通知を管理画面内へそろえた。共通層でも登録解除失敗後の保存値保持/再試行を検査する。Swift/UI実行はまだ残る。

P0-1.lifetime/P0-3.failure/P0-5.ownershipはCore/Integration unit、P0-1.integration/P0-3.integrationは既存P0-Aの生成比較を再実行。P0-2.presentation/uikitは提示fixture、P0-5.uiは通常管理試験と失敗fixtureを組み合わせる。
P0-3.device/P0-5.deviceと提示/UIKitの実機確認は0.7.0へまとめる。Widget反映/Shortcut拒否もその操作表へ含める。
凍結時にcheck-delivery reportのsource・P0-A依存証拠・全selector・jobs・初回2 run予算を照合する。source検査、Python工具tests、実Swift結果、実機証拠を混同しない。

## 初回CI前の一括レビュー

管理層のawait前受付停止、保存予約競合時の未完了維持、登録解除/削除失敗後のデータ保持、実Task終了とUI dismiss完了の待合せを確認。通常入口（一覧/URL/通知/scene/Shortcut）、Widget共有状態読取、通常バックアップの保存予約への接続を確認した。
提示callbackの世代と同時終了、UIKit coordinator更新、初期データ読込前のUI assertion、長いListの入口表示、削除確認後のinline失敗表示を既存指摘とまとめてレビューした。製品ソースを変更するためP0-Aの試験成功をそのままP0-B合格に転用しない。
初回は通常/生成2 runを維持。担当ごとのCI起動は0。候補固定後のSwift型検査と実行結果はCIで記録し、実機の確認は0.7.0へ予約する。

## 通常CI34695571482の結果と修正

source `5cffdf2df4a4dc57b53458c51801e6583b99f956`、[run](https://github.com/y-aplus/JibunKit/actions/runs/34695571482)、job3分59秒。共通239 tests（2 skipped、失敗0）、Records11 tests（失敗0）、native build requirements検査は成功。
通常hostのReleaseコンパイルでJibunKitAppのネストしたhandler構築にSwiftの診断生成失敗、MiniAppRegistryのstatic管理初期化にMainActor/nonisolated default argumentエラーが出た。IPAとUI testsは未実行であり、このrunで通常動作を合格としない。
MainActorのRegistry factoryへ管理/dispatcher構築を分離し、host phase handlerの型と管理のcoordinator/storageKeyを明示した。scene dispatcherは各ViewのStateで個別に生成するままで、管理状態の動的判定、イベント順序、ownerの寿命を変更しない。
初回2 run計画に通常コンパイル修正後の再検証1 runを加えて、実行予算を3に更新する。再検証は通常全範囲を維持し、その成功後に同じ候補の生成比較を実施する。

## 生成比較34697206948の結果と提示元の保持

source `2ed4f422fc7de5f70205520d565b6b950ddf1af2`、[run](https://github.com/y-aplus/JibunKit/actions/runs/34697206948)、job27分48秒。共通239試験（skip2）/Records11、生成/通常app・widget/IPAとNotes単独が成功。生成hostの8件中、P0-A寿命2件/Records保守/SQLite保守/独立Records保存の5件が成功、P0-Bの3件が失敗した。

管理試験は失敗理由の保存値まで確認後、背後のランチャーCollectionViewをスクロールして管理側のstatusを探していた。管理Listへ識別子を付けて対象を限定し、失敗段階の期待文字列も製品の実際の文言へ合わせた。
提示試験では、URL切替時にAのrootが消えてsheetの終了通知が完結せず、Aへ戻るとsheetが残った。UIKit内からstopした場合も、lifetime wrapperがstoppingへ移るとrootを外してonDismissを受け取れず、終了待ちのままになった。これはunit成功では検出できなかったホスト接続の欠落であり、timeout延長やonDisappearを完了と扱う修正はしない。

Definitionへ任意の所有presentation接続を追加し、AppNavigationは旧rootを維持してdismissForNavigationのnative完了を待ってから最後に要求されたownerへ移る。通常の画面切替ではRuntimeの接続を維持し、戻った後の提示を許可する。Runtime終了と同時なら終了側が世代を閉じる。lifetime wrapperはrunningとstoppingで同じcontentの構造を維持し、操作を止めて完了を待つ。無効化中も既に表示しているroot/pathは受付とは別に後始末まで保持する。
既存UIKit stop/外部URL/取消/B保持UIをそのまま回帰に使い、navigation後の再提示と同時shutdownの実終了待ちをCore試験へ追加した。

通常CI34695873582は旧sourceで共通239/Records11/通常UI13/Files JSON復元成功、job27分20秒。ただし今回のhost/Core変更で通常証拠の再利用はできない。まず生成比較全体で3失敗とP0-A回帰を再検証し、その候補で通常全回帰を再実行する。実run予算は5へ更新（通常失敗/成功、生成失敗/修正後生成/修正後通常）。各jobは見込み30分、timeout45分を維持し、担当ごとのCIは起動しない。
