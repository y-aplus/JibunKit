# P0-B 画面・同意・無効化/削除

2026-09-12。実装中、Swift/iOS未検証。P0-A候補cd45ea8から分岐し、そのCI完了通知を待つ間に独立部分を進める。

## 現在の実装

- Feature別同意宣言/保存とCounter/Reminder削除providerを別担当から取り込んだ。データ形式/キーを変更せず所有キーだけ削除する。
- 共通管理層は最初のawait前に起動受付を閉じ、無効化/削除の意図をowner別に永続化。保存領域の排他予約、Runtime終了、所有登録解除、削除、同意消去の順で処理する。
- 無効化はデータ/同意を保持する。削除は再登録まで起動を拒否し、再登録で以前の同意を復活させない。解除/削除は再試行可能であることをcallback契約とする。
- エラーでは途中状態と失敗段階を残す。再起動でも途中状態を有効に戻さない。終了が進まなければ待機処理/解放件数を管理画面で表示する。
- 通常hostの一覧、経路解決、通知、scene/hostイベントに受付条件を接続。cold launchのネイティブ登録自体は必要時刻に行い、そこからの業務処理はFeature lifetime経由の契約を維持する。
- アプリ内管理画面に無効化、再有効化、対象/データ説明と確認付き削除、再試行、利用同意変更を追加。リマインダーの通知操作に目的/拒否時動作の説明と同意判定を接続。
- Counter更新、Reminder保存/通知の通常ボタン処理をRuntime所有にして、削除前の終了待ちへ含める。

## 検証と残件

Core tests追加: Aの無効化でデータ保持、削除と再登録で同意初期化/B保持、削除失敗後の再起動と再試行、保存予約競合時の保持。
保存・snapshot・restoreの共通予約も、無効/削除中/削除済みownerの新規受付を拒否する。管理の後始末だけが専用の内部予約で進める。
この受付は管理層初期化中に同期設定し、保存済みの途中状態をcold launchのイベントに先行して適用する。
Core testsに、削除後の通常アクセス/保守拒否、再登録後の受付回復、破損した管理状態の起動拒否と他owner保持を追加。
通常UI testはCounterの更新→無効化→再起動→URL拒否→再有効化→削除取消→値保持→削除→再登録で0→Reminder保持を通す。
既存の通知UI testにはFeature同意の説明画面を追加した。
これらは未実行。WindowsのPythonツール検証はSwiftやUIKitの証拠に代えない。

P0-B初回CIまでに、管理/拒否/取消の通常UIテスト、提示中の外部遷移、UIKit終了、画面保持/再生成/reset契約を一式にする。
提示担当の4da3678/5eb215eを一括レビュー後に統合。Runtime世代付きhandle、同時終了の共有、closed Runtimeの提示拒否、旧世代cleanupから新世代の保護を実装した。
親はdidEndでUI handleが消えた後も非同期dismiss callbackが実行中なら終了待ちに含める条件を追加した。生成hostのP0BPresentationUITestsでSwiftUI提示/取消、提示中URL遷移、UIKit内からの外側stop→dismiss完了→再開、B保持を検査する。
通常管理UI testsにFeature同意の拒否→通知再操作の拒否→通常メッセージ編集/保存の維持→同意変更を追加。これらのSwift/UI試験は未実行。
別sceneで既に開いたbackupは同じcoordinatorの予約時に拒否する。別processやOS外部入口（Widget/Intentを含む）からの受付との整合は引き続きレビューし、未接続入口を有効化済みと扱わない。
通常Counter Shortcutはcold launchでも管理状態を初期化して有効性を確認し、更新を同じowner保存予約へ接続した。無効化/削除後の更新は拒否する。実OS Shortcutでの拒否・再有効化後の回復は候補検証に含める。Widgetの表示更新と共有状態はまだ確認が残る。
管理状態の保存先を、通常hostでは既存App Group解決による共有defaultsへ接続。Widgetは同じ状態を読み、無効/削除済みなら保存値を表示せず、状態変更時にhostからtimeline更新を要求する。共有設定が解決できないhostではlocal管理を維持し、Widget側は共有領域が読めない表示となる。
独立したdefaults readerでAだけの状態変更を読むunit testを追加した。これは別processのOS更新時刻の証拠ではなく、Widgetの反映時刻はOS調停に依存する。既存0.6.0には管理状態キーがなく、Feature保存/backup形式は変更していない。候補のWidget/Shortcut確認は0.7.0へまとめる。
所有Runtimeを経由しない任意Featureコードの強制停止や、同一processのセキュリティ隔離は提供しない。
通常UIと生成比較を統合レビューしてから、P0-Bの大きなCI境界で実行する。小変更ごとのCIは起動していない。

0.7.0候補時に実機操作をまとめ、別途進行中の需要調査が未反映ならユーザーに提供を依頼する。
