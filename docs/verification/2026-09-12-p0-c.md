# P0-C 追加・更新経路と0.7.0候補

2026-09-13。実装中。P0-A/P0-Bの非実機条件を検証してmain 36bda79へ統合し、その状態を本branchへ取り込んだ。P0-Cの診断・追加・互換更新を一括CIへ進める。0.7.0の実機確認・文書監査・配布確認は未完了。
[契約](../delivery/P0-C-contract.md) / [前提P0-Bの証拠](2026-09-12-p0-b.md)。

## 実装した範囲

- `check-feature-connection.py`は標準の`swift package dump-package`と`tuist dump`を呼び、ローカルPackageのpath、library product、所属target、指定hostの直接product依存を診断。Swiftソースを正規表現で推定しない。推移的Integration依存やコンパイル、Registry登録の保証はしない。
- Tuist 4.207.0の[Package](https://github.com/tuist/tuist/blob/4.207.0/cli/Sources/ProjectDescription/Package.swift)、[Path](https://github.com/tuist/tuist/blob/4.207.0/cli/Sources/ProjectDescription/Path.swift)、[TargetDependency](https://github.com/tuist/tuist/blob/4.207.0/cli/Sources/ProjectDescription/TargetDependency.swift)のCodable形を根拠にし、未知の形は診断不能として失敗にする。現在のPython試験データは手作りで、実Tuist出力の試験はまだ残る。
- `verify-feature-connection.py`で小さいnative manifestを作り、正常→path欠落/product誤名/target欠落/host依存欠落を別々に評価→修正後正常の操作列を用意した。既存CI stepへまとめて接続し、失敗例ごとにrunを作らない。
- `MiniAppValidator.validate(ids:expectedIDs:)`で実際のRegistryに必要なIDがあるかを診断。期待集合外のFeatureは許容し、従来の重複診断を維持する。P0COnboardingProbe/UITestsは生成hostの実Registryを検査してNotesの通常URLへ進む。
- 文書担当は2文書の一括提出`11f556e`。親がレビューし、診断コマンドとRegistryの確認範囲を追記した。個別CIは0。薄いIntegration、P0-Aの寿命/保存接続、P0-Bの今後の管理接続、二Packageの片側互換更新と復旧を手順へ記載した。

## 実行済みと残件

ローカルPython工具27試験、P0C selector、diff whitespaceは成功。Swift/Xcode/TuistはこのWindowsで実行できず、native manifest診断・新しいCore試験・生成UIは未実行。

- [x] P0-Bの生成比較成功と必要修正を作業branchへ統合した。
- [x] P0-Bの修正後通常全回帰34702137986を確定し、mainへ統合した。
- [x] 二Packageのresource/翻訳と保存の片側互換更新を生成hostへ接続した。
- [ ] そのnative/iOS実行結果を確認する。文章や準備成功だけでP0-4.onboardingを合格にしない。
- [ ] 正常/期待失敗のnative診断を実Tuist/Swift出力で検証する。
- [x] P0全体の証拠を差分と照合し、初回2 run上限内の1 runへ集約した（末尾の凍結計画を優先）。
- [ ] P0-C一括CIと、0.7.0候補の実機確認、minor全文書監査、候補IPA再取得/整合性確認。
- [ ] 0.7.0完了時に別途調査が未反映ならユーザーへ結果提供を要求する。

候補はP0-Bから先に完成扱いせず、実機確認は小タスクごとに依頼しない。公開版は引き続き0.6.0。

## 互換更新のnative検証を統合

担当の`af6b7f0`を一括レビューして統合。固定fixtureのA v1/B v1を別Packageへ構成し、同じ保存先へv1で書込→Aのみv2へソース更新して別processで旧値読込/追加field保存再読込→破損Aを別processで拒否しbytes保持の3段階を実行する。両Packageの同名JSON/翻訳値とBの保存bytes/Package tree、A/Bのresource treeを比較する。
親は上書き前のA v1 JSON実物の保管を補い、通常のnative検証stepと診断artifactへ接続した。artifactは生成source/manifest、保存before/after、実行ログ、結果JSONで、Runnerの.buildは含めない。既存証拠のあるoutputを上書きせず停止する。
WindowsのPython27試験と構文/diff確認のみ実行済み。native Swift実行・iOS更新インストールは未実行で、P0-4.onboardingの実画面を代替しない。既存iOS resource/Records経路との結合とP0-B結果の反映は引き続き残る。担当別CIは0、一括提出に対する親の局所補完のみで追加往復はしていない。

## 2026-09-13の統合と残るCI設計

P0-B `d1adb8e`を9e189e1で作業branchへ統合した。Counterのlifetime Environment接続は検証済みP0-Bと同一に解決し、workflowはP0-Bの提示/管理fixtureとP0-Cの診断stepを両方保持した。文書の競合は古い実行中表示を最新の生成成功/通常回帰待ちへ更新し、失敗と修正の経過を残した。公開mainはまだ変更しない。

native接続診断は、各ケースのProject.swift/Package.swift、実stdout/stderr、終了コードと期待診断をartifactへ保存する。期待失敗を単なる非ゼロ終了で合格にせず、欠落箇所の診断文字列も確認する。既存evidence directoryへの上書きは拒否する。
生成hostのRegistry試験には、Notes productはリンクしたまま実Registryから定義を省く起動→欠落IDと修正ファイルの診断→Notes URLで開けない→登録を戻して再起動→診断正常/通常Notes URLで開く、を追加した。fault injectionは一時生成hostだけで、通常IPAへ持ち込まない。Swift/iOS未実行。

次のCIは初回2 runを維持する。最終selectorと見込み時間はiOS互換更新fixtureの一括提出後に固定し、現段階では起動しない。

| 証拠群 | P0-Cでの扱いと条件 |
| --- | --- |
| P0-A寿命/Records/SQLite、P0-B提示/管理失敗の生成8件 | d1adb8eの34700435807を元証拠とする。現時点でP0-Cの製品コード差分はValidatorの任意expectedIDs診断だけで、既定の呼出動作とhost/lifetime/storage/managementは不変。凍結時にも差分を確認し、影響が生じたケースだけ再実行する |
| 通常全UI/Files/管理 | 修正後の34702137986の結果を待つ。P0-Cの通常候補でも共通/Module試験、通常IPA/hostと通常回帰を一括実行し、0.7.0候補の証拠へ接続する |
| Package追加・Registry漏れ・片側更新 | 生成hostのP0-C操作列に時間を割り当てる。片側更新はv1で実保存→A sourceのみv2へ変更→同じSimulator/containerへ再install→旧値/追加field/再起動/B保持/破損拒否・復旧までまとめる |
| Package resourceの従来4構成比較 | P0-Cで同名resource機構自体を変更しないため、従来比較を無条件に全実行しない。新しいA更新操作の各段階ではA/B resource/翻訳値を実際に検査する |
| 実機/文書/配布 | 0.7.0候補へまとめる。CI成功で実機やminor文書監査を代用せず、別途調査が未反映なら完了時に提供を要求する |

更新fixtureは既存の別スレッドへSol lowで一括依頼し、担当単独のCIは起動しない。親は同時にnative診断の証拠保存、Registryの期待失敗、P0-B統合とCIの再利用条件を進めた。

## iOS再インストールfixtureの一括統合

担当提出88041daを5f9a4ebで統合。親がsourceの変更範囲、v1とv2の保存処理、通常Feature URL、再起動の操作列を一括レビューし、v1の既存ファイル再投入拒否、破損データの実ファイル読取、修復前の再起動後bytes確認、最初のUI失敗で停止する設定を補った。workerのCIは0。

生成hostのProjectはTests/PackageResourcesのA/Bを直接依存し、Registryは更新probeを接続する。prepare v1後のB/source/resource/host/UITest hashesを固定し、v2はA StoredValue実装だけ差し替える。準備スクリプトを元checkoutの絶対パスから呼ぶことで、生成host自身をsourceと誤認して拒否する問題も避けた。これは文字列接続チェックだけでコンパイルを保証するものではなく、実manifest診断と後続native buildを別に実行する。

選択する初回2 runは以下。P0-B通常回帰を確定してから候補SHAとreportを固定し、実行する。

| run | 入力と操作 | 見込み / timeout |
| --- | --- | --- |
| 通常候補 | simulator_tests=true、feature_validation=false、filters空。他のboolはfalse。共通/Module・新expectedIDs試験、native診断/互換更新、通常app/widget/IPA/Counter・UI13/Files | 30分 / 45分 |
| 追加・更新 | simulator_tests=true、feature_validation=true、ui_test_filter=MigrationUITests/MigrationUITests/testMiniAppLinksOpenColdAndSwitchWarm、feature_ui_test_filter=MigrationUITests/P0COnboardingUITests,MigrationUITests/P0CCompatibleUpdateV1UITests/testSeedV1ThroughNormalFeatureURL。他のboolはfalse。Notes生成/単独・host登録漏れ修復、A/B v1保存後に同じjobでAのみ更新しV2の実再install・再起動・破損拒否・修復/B保持 | 30分 / 45分 |

V2 selectorは`MigrationUITests/P0CCompatibleUpdateV2UITests/testReadUpdateRelaunchRejectCorruptionAndRepairWithoutReseeding`を更新stepが明示指定する。V2だけを初回filterへ渡すことは拒否し、先行v1成功と実containerのA/B取得がなければ更新へ進まない。実appをアンインストールせず、同じSimulator・bundle ID・DerivedDataを使用する。v1/v2のA/B JSON実物、source invariant、nativeログ、両xcresultを保存し、最後に旧A/更新後Aの内容とB exact bytesをCI側でも照合する。

見込みはP0-Bの生成job30分09秒から既存8件（約400秒のケース実行＋suite起動等）を外し、新診断と新しい2段階更新へ置き換える前提。通常は既存27分20秒にnative診断等を加える。実績は初回実行後に記録し、30分を超えたら超過を隠さず次境界を調整する。

ローカルは工具27＋準備2試験が成功。workflow内の実Python注入をgit archiveの一時hostへ実行し、実Registry injectionと正しいA/B path、そのhostでのv1→v2準備まで成功した。両selector、Python構文、diffも確認済み。Swift/Tuist生成/iOS再install/UIはまだ未実行で、P0-4.onboardingを完了にしない。

## 34702137986確定後のCI凍結計画（最新）

P0-Bの同source通常回帰は29分35秒で成功し、main 36bda79へ統合した。P0-Cとの差分を再確認すると、Sourcesの製品変更はMiniAppValidatorの任意expectedIDs（既定空集合）だけで、通常host、Counter/Reminder、管理、寿命、提示、保存/復元の実装は同一。Projectは本体/Widgetの0.7.0 build 8化だけ。生成host・診断tool・fixtureの変更は通常IPAのFeature構成を変えない。

このため直前の通常13件とFiles選択復元をd1adb8e/34702137986から再利用し、以前の初回2 run予定を**初回1 run（上限2）**へ集約する。残る全native/Core/Module試験、生成・単独・host、Package追加診断/登録漏れ修復、片側更新v1→v2、通常app/widget・IPA検査、通常URL回帰は次の同じjobで実行する。

```
simulator_tests=true
feature_validation=true
ui_test_filter=MigrationUITests/MigrationUITests/testMiniAppLinksOpenColdAndSwitchWarm
feature_ui_test_filter=MigrationUITests/P0COnboardingUITests,MigrationUITests/P0CCompatibleUpdateV1UITests/testSeedV1ThroughNormalFeatureURL
```

他boolはfalse、runtime空。V2は同job内の次stepで明示selectorを実行する。見込み30分/timeout45分。P0-A/P0-B生成8件は34700435807から、終了診断/局所処理のP0-6は既存unit・ガイドの根拠から再利用し、reportに元sourceと無変更の理由を残す。新しいnative/生成の失敗や製品修正が生じたら影響範囲を再判定し、再利用を固定しない。

実機確認時の追加ビルドを避けるため、同じ生成Release buildに検証済みのP0-A寿命/Records/SQLiteとP0-B提示/管理失敗probeを登録する。旧UI全件は再実行せず、生成buildで型検査する。通常IPAと別artifactの`JibunKit-P0-check.ipa`へad-hoc署名・ZIP/CRC検査して保存し、確認画面へ操作できるようにする。これは確認専用版で、通常の配布IPAはCounter/Reminderのみ。候補0.7.0 build 8は未公開であり、実機/文書/配布/研究結果の条件が残る。

## 初回34704117433のコンパイル失敗と修正

source `e84402f9154688664fae15080d5bb5b393fd4798`、[run](https://github.com/y-aplus/JibunKit/actions/runs/34704117433)、job1分29秒。共有試験のコンパイル中、追加したMiniAppRegistryExpectationTestsが`.counter`/`.reminder`を参照して失敗した。この略記はJibunKitCoreではなく各Featureのextensionで定義され、Coreだけをimportする試験からは利用できない。enum caseの型推論エラーもその後続診断だった。

試験のIDを公開の`MiniAppID("counter")`/`MiniAppID("reminder")`へ変更した。同じ略記を使った生成hostのP0COnboardingProbeも直した。CoreへFeature依存を追加せず、比較するID集合と期待結果、実際のRegistry漏れ/修復操作は維持する。製品コードやnormal hostは変更しないためP0-Bの証拠再利用条件は変わらない。

このrunでは試験実行・native manifest診断・生成host・IPAまで進んでいない。Python事前成功をSwift成功として数えない。再検証は凍結済みの一括selector全体を維持し、実run計画を2へ更新する（初回上限2以内）。文書監査は独立branchで継続中。

## 34704681281のnative診断判定と文書監査

source `81a4bb7`、[run](https://github.com/y-aplus/JibunKit/actions/runs/34704681281)。共通244試験（skip2、失敗0）が成功し、Core-only ID修正を確認した。nativeビルド要求の合成/拒否、URL宣言、app/widgetのlocalized Info.plist・署名entitlement読戻し、接続診断の正常/path欠落/product誤名も成功。

library target欠落はSwiftのdump-package自身が`target 'NotesFeature' referenced in product 'NotesFeature' could not be found`として正しく拒否した。checkerはそのnativeエラーを保持して失敗したが、試験はchecker内部の`missing target NotesFeature`だけを期待していた。nativeが先に拒否する段階を誤って想定した検証不備であり、その実際のtarget/product名を含む拒否文を期待するように修正した。一般的な非ゼロ終了なら合格とする変更ではない。

接続診断は六ケースをすべて走らせて不一致をまとめて失敗にする。workflowの独立したnative requirement/connection/compatible-update/privacy検査も各終了コードを保持し、最後に全体を失敗にする。互いに独立した不具合を一回で観測し、後続生成/iOS検証は全native成功後にだけ進む。ローカルmockで六ケースを最後まで実行し、期待外の結果では全体失敗することを確認した。native Swift/iOSの再実行はCI待ち。

文書担当173534dの52文書監査（14更新、38理由付き据置）を一括レビューして統合。親がP0/P1混在表の見出し、ZIPFoundationを含む表の同梱説明、六つの現在文書のCI状態を補正し、監査hashを更新した。Package.swiftのZIPFoundation依存とlicense resourceを照合済み。minorの公開前・公開後に変わる状態やsourceは引き続き再確認し、今回の監査だけでrelease gateを完了にしない。

実run計画は3へ更新し初回上限2の超過を記録する。製品の通常動作は変更せず、修正したnative判定・全診断の実行・生成host/iOS更新・IPAを同じ大きなCIで再検証する。P0-B通常/生成の再利用と実機予約は維持する。
今回のjob実測は2分38秒。Records package、Package互換更新、生成hostと候補IPAはこのrunで未実行。
