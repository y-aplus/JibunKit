# 0.8候補の一括実機確認（準備中）

現在は手順案。実行依頼・成功記録ではない。候補のCI、IPAのCRC/署名/配布byte照合が終わってから、実ファイルへの直リンクとともに会話へ確認事項を提示する。GitHubの表示だけを読む必要はない。

診断IPAはsource `6beb87760ffbd0dedd7aab9f98dee3dcde6b8604`、[CI34816553410のartifact](https://github.com/y-aplus/JibunKit/actions/runs/34816553410/artifacts/10337591637)。生成・署名検査・ダウンロード後CRC/SHA256照合は完了。HTTP/通知3件、固定portを含む通信7件、Web4件が成功した。続く[CI34819734774](https://github.com/y-aplus/JibunKit/actions/runs/34819734774)ではWeb3件成功、無効化のSpotlight応答待ちとその状態を引き継ぐ認証準備の2件が失敗。[native直接比較34822983863](https://github.com/y-aplus/JibunKit/actions/runs/34822983863)は5操作成功。現在は[同一host比較34823801940](https://github.com/y-aplus/JibunKit/actions/runs/34823801940)で無効化・認証と直接domain削除を切り分ける段階であり、実機以外が完了した状態ではない。製品の解除処理の変更はなく、現時点のIPAを保持する。新しく追加した比較用ボタンとDebugログはこのIPAには含まれない。公開直リンクによる配布はまだ行っていない。

診断IPAはP1-A（Intents/Widget/受信）とP1-B（通知/HTTP/Web）を含む。HTTP/Web認証は端末内で動作し、PCのserverは不要。表示versionは0.7.0/build8であり、0.8.0公開を意味しない。

配布前に確認するもの：全jobの結果と必要なmethod証拠、診断/戻し用通常IPAのbundle IDと署名、両方のダウンロード一致。現在のCounter/Reminderを控えてバックアップを保存し、同じアプリへ上書きする。以下の削除対象は診断Featureだけ。通常版へ戻すのは最後にまとめる。

## 1. ShortcutsとWidget

- アプリの「Intent診断A/B」で候補名を別々に保存する。OSのショートカットでJibunKitの`Add Feature A value`/`Add Feature B value`（短い表示は`Add A/B`）を見つけ、Amountを指定して実行する。結果の数値を表示するアクションにつなぎ、アプリで「値と候補を再読込」した値と一致するか確認する。A操作中にBが変化しないことも確認する。
- `Read Feature A entry`/`Read Feature B entry`のEntry候補が各Featureで保存したものだけになり、選んだ名前が戻るか確認する。診断画面の「1を追加」だけではOS入口の確認を代替しない。
- ホーム画面にJibunKitの「機能A」「機能B」Widgetを置き、アプリの同名画面から各値を別々に増やす。表示更新後にAだけを無効化→再有効化→削除する。Bの数値とWidgetを保持し、A削除後の再登録では古い値が復活しないことを確認する。即時更新を前提にせず、更新しない場合は経過時間と表示を記録する。

## 2. OSの共有から受信

「受信検証A/B」の「OS共有シート」から文字列・URL・ファイルを共有し、共有先JibunKitで受信先を選ぶ。選んだ側だけに内容が入り、もう一方を変えないことを確認する。一度は共有を取消し、新規取込みが増えないことも確認する。アプリを終了して開き直し、保存済みの内容を確認する。「A/Bへ受信を保存」は内部投入のため、このOS確認には使わない。

## 3. 通知の操作・添付・他方保持

「通知検証A/B」で通知を許可する。Aで「5秒後に配信」を押したらホーム画面へ戻り、届いたカードを長押しして画像と「記録」「返信」を確認する。

- 「記録」後、Aの「実データを再読込」で`p1-notification-a action=mark text=none`。
- Aをもう一度配信し、「返信」で`check-A`を送信。Aで`p1-notification-a action=reply text=check-A`。Bの操作記録は変わらない。
- Bを配信し、その通知カードは操作せず残す。Aの「この所有者の通知を解除」を実行しても、通知センターのBカードが残ることを確認する。Aの管理削除後もBを保持する。
- 再登録したAとBで、アプリを開いたまま別々に配信する。Aはバナーなしで`policy=silent`、Bはバナーありで`policy=banner`。表示先が別Featureでも方針の所有先が入れ替わらないことを確認する。

集中モード等で配信を確認できなかった場合は成功とせず、操作できなかった範囲を記録する。文字列一致とOSカードの実動作を別々に確認する。

## 4. HTTP/Webと認証

- HTTPのA/Bを開き、それぞれ`Login and persist`。A/B別のCookie/認証値が出ることを確認し、その起動中に`Read owner cache`で各所有者の応答を確認する。アプリ再起動後は`Reload persisted HTTP state`でCookie/認証を確認する。URLCacheはOSによる破棄があり、再起動・上書き・署名更新をまたぐ永続性の必須条件にはしない。これは仮の診断資格情報で、実アカウントの入力は不要。
- Aで`Hold writer`→`Cancel held writer`。`cancelled held-write; no late cookie`となり、元のCookieを保持することを確認する。
- WebのA/Bで「保存」。再起動後に「読込」し、local/cookie/indexeddbがそのownerのままであることを確認する。
- WebのA/Bで「認証開始」し、OSページの`Return to App`を押す。各々`completed p1-web-a/b`。Bの「認証取消検証」でOSの閉じる操作を行い、Bが`cancelled`、Aの結果は保持されることを確認する。

## 5. 上書き・署名更新と片側logout

4のA/Bデータを保持したまま同じ診断IPAをSideStoreで上書きし、HTTP/Webの読込とWidget/受信の保存値を確認する。SideStoreの署名更新も行い、同じ確認をする。上書きと署名更新を行えた範囲を区別して記録する。

HTTP Aで`Stop writers, then logout`→画面を開き直す→`Reload persisted HTTP state`。Aが空Cookie/`unauthenticated`となり、Bは保持されることを確認する。Web Aだけを管理から削除し、Bの保存値を保持することを確認する。

最後に案内する通常IPAへ上書きし、Counter/Reminderと既存の通常Widget/Shortcutを確認する。診断Featureが一覧からなくなることは候補だけの構成による。これまでの0.7実機確認を、新しいP1の成功として転記しない。

## 記録と残る判断

返信は各番号の結果と、実施できなかった小項目を分けて記録する。予期しない結果は操作・表示全文を残す。まだ全項目未実施。CIの新しい結果を確認して手順と期待値を確定する。

0.8完成時には現況文書をマイナー版境界で監査し、Issue #6を踏まえた1.0の正式境界をユーザーと確定する。この手順の完了だけで1.0完成とはしない。
