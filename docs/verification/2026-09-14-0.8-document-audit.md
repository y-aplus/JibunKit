# 0.8文書監査（進行中）

2026-09-14、実装比較source `e6019fec58314098164f87c457e36f8b282f623f`。公開版は0.7.0。これは文書の部分監査であり、0.8出荷や全ファイル確認の記録ではない。対象全体は`python3 Tools/check-delivery.py --list-docs 0.8.0`で取得し、minor公開前に各ファイルの内容と証拠を照合する。

今回確認したファイル：

- `docs/guides/spotlight-ownership.md`: Coreのnamespace/index引数とRegistryのdefault index解除に照合。hostがcustom indexを自動生成するという誤記を修正し、custom indexはonUnregisterへ接続すること、未完了のnative応答待ちを追記。
- `docs/guides/feature-management.md`: MiniAppManagementのdisabling/removing永続化、stagesの解除、enable拒否と管理画面の再試行ボタンに照合。実行中と失敗終了後を区別。
- `docs/guides/feature-data-removal.md`: removeDataの呼出順、consent削除、enableの処理に照合。アプリ内同意の初期化とOS権限が残ることを区別。
- `docs/status.md`: Issue #6は受領済みで、正式1.0境界を0.8完了時に決めるというユーザー判断に更新。公開0.7.0/P1-A main/P1-B未統合の区別を維持。
- `docs/verification/2026-09-14-0.8-device-check.md`: run9の失敗とnative比較を反映。候補の配布/実機依頼済みとは記載しない。これは現在の実行手順案であり、完了済みの履歴を変更したものではない。

製品/fixtureの挙動は変更していないため、この文書差分だけの新しいCIは起動しない。全対象ファイル、0.8 release notesと版表示、実機結果、最終的な同一sourceへの証拠対応はまだ未完。公開前にはこの部分確認以降の実装差分も再確認する。

## HTTP/Web/通知ガイドの追加確認

比較source `d6b6d265602b3b066a9fc8e3ad435134bc81e964`。同一host比較CI34823801940の待機中に、別文書branchで確認した。実行中CIのsourceは変更しない。

- `docs/guides/web-storage-ownership.md`: 管理がRuntimeを停止してから排他予約を取る実装に順序を修正。新しいloopback候補で成功した保存/取消/drain3件と、未解決のSpotlight無効化失敗を区別。
- `docs/guides/feature-http.md`: device候補は端末内Network.framework serverで動く。外部Python serverだけを前提とする説明を修正。run8のnetwork job成功とrun全体failureを区別。
- `docs/guides/notification-attachments.md`: 冒頭のSwift/native検証未実施という古い記載を修正。Foundation・native UIと未実施のOSカード操作を区別。
- `docs/guides/web-authentication-ownership.md`: 端末単体の認証ページ、run8の通常host認証成功、run9の管理準備失敗と残る実機条件を反映。

`notification-request-snapshots.md`もCoreのsnapshot型/host配送と照合したが、今回修正は不要。文書の操作契約の確認であり、新しいSwift/native試験の成功として扱わない。

## 正本の同期と運用変更（2026-09-14）

run34823801940完了後、plan.jsonのP1-4/5/6を実装・検証済み範囲と実機残件へ同期。status/CHANGELOGへP1-A mainとP1-B開発branchを区別して反映。優先順位文書の重複進捗表は正本への参照に整理し、D台帳のWeb削除調停・通知添付の古い未実装説明を訂正した。公開手順の調査未提出扱いも解消。

ユーザーの新指示により、実機確認した成果ごとに版を進めること、遅くとも3失敗までの切り分けを公開/CI/並列運用へ反映。文書の正本・要約・利用手順・履歴の役割をCI手順へ明記した。これはminor全件監査の完了ではない。

廃止提案は2026-09-14にユーザー承認済み。minorの全件レビューは維持し、文書ごとのSHA-256/個別理由の別台帳への重複記録を、確認Git commit・全件のpath/outcome一覧・まとめた更新/非更新理由と変更点・未解決事項へ置換した。check-deliveryは確認commitと現在文書の一致を検査する。0.8.0の全件レビューは引き続き未完了。

過去運用の確認: 0.7.0のP0-C evidenceには52文書（updated 21、reviewed-unchanged 31）の個別理由/hashがあり、出荷記録にも全件確認を記載している。ただし記録だけで読解の十分さは証明できない。このgateはminor出荷時の記録漏れ・確認後の変更を検出するもので、文章の意味の正しさやその後の開発中の同期を保証しない。今回のP1でのUnreleased、計画の残作業、検証済み範囲、調査受領状態の同期漏れは別途修正した。個別記録があれば内容更新漏れも起きない、という扱いにはしない。

変更検証: `python -m unittest discover -s Tools/tests -p test_delivery.py -v`の13件が成功。実Gitの一時repositoryで文書編集、確認一覧の欠落、確認commitに未収録の追加文書、無効なcommit/まとめ欄を検出し、再確認後に通過することを検証した。既存P1-B evidenceのpreflightと`git diff --check`も成功。iOS実装/試験は変更しておらず追加CIは起動しない。
