# P1-B実装・検証記録

## 10回目：独立native比較は成功、通常hostの切り分けへ

[34822983863](https://github.com/y-aplus/JibunKit/actions/runs/34822983863)、source `a8c91188e2ff0f14764ff39af4b6348e92b7920e` はsuccess。job3.90分、XCTest1件11.689秒。初回空domain削除1.108秒、再削除2.075秒、async overlay4.565秒、index登録0.006秒、登録済みdomain削除3.929秒の5操作でcallback error=nil、期限内完了。artifactのtests.logと個別結果を照合した。JibunKitを使わないアプリでも必ず初回削除が止まるという仮説は再現しなかった。通常hostの管理停止の原因は未確定。

実装source `d6b6d265602b3b066a9fc8e3ad435134bc81e964`。通常RegistryのDebug buildに所有者と解除段階の開始/完了ログを加え、native完了が返るか、返った後のhost側で止まるかを区別する。Releaseではこのログコードを除外する。既存Spotlight診断画面にも管理を経由しないnative/namespaceのdomain削除比較を追加した。

11回目は同じ完全P1 hostで、Web無効化・認証・直接Spotlight比較の3 UI方法を検証する。比較を最後に実行し、問題の最初の無効化を先に暖機しない。前回成功のWeb保存/取消/drain3件、run8のHTTP/通知と通信7件、run7のP0を再利用する。見込み22分/上限45分。全体の繰返しと専用native jobは行わない。前ターンの未完了管理の通常再試行helperもここで初めてiOS検証する。Tools55件、actionlint、diff成功。

診断比較用の画面追加とDebugログ以外に製品の解除契約やHTTP/Web実装の変更はない。6beb877の配布候補IPAを保持するが、新しい比較ボタンはそのIPAには含まれない。実機以外の受入・一括実機依頼・0.8公開は引き続き未完。


## 9回目：Spotlight登録解除での停止を特定

[34819734774](https://github.com/y-aplus/JibunKit/actions/runs/34819734774)、source `b62a2e635993900b47cf3738570b3f1d2825893b` はfailure、Web job28.10分。書込取消136.873秒、管理による書込drain/削除152.546秒、再起動保持/片側削除219.107秒は成功。無効化245.850秒と認証154.268秒は失敗した。

Simulatorログでは08:03:19.206に通知取得開始、.212にpending0件、.245にdelivered0件が返り、.486にcategory4件を設定。その後.490にCore Spotlightのdomain削除、.493にIndexAgentへのXPC接続開始が記録され、08:05:29でも管理は登録解除中だった。120秒への延長では解消していない。認証試験は前の未完了disablingを引き継ぎ、存在しないenableボタンを探して準備段階で失敗した。認証API失敗とは数えない。

実装source `e6019fec58314098164f87c457e36f8b282f623f`。JibunKitをimportしない小さなiOS hostを追加し、初回のnative空domain削除、2回目、async overlay、index登録、登録済みdomain削除の5操作を各30秒で比較する。期限超過は失敗のまま記録し、後続操作の回復有無を測る。これはOS直接操作の比較であり、受入成功を代用する試験ではない。API契約は[Appleのdomain削除](https://developer.apple.com/documentation/corespotlight/cssearchableindex/deletesearchableitems(withdomainidentifiers:completionhandler:))に従い、製品の削除待ちを省略しない。

10回目はnative比較のみ（見込み6分、job上限30分）。全体UIを再実行する前に、CoreSpotlight自身の初回応答を切り分ける。Webテスト準備にも通常画面の再試行ボタンで未完了状態を回復する処理を追加したが、そのUI変更のiOS検証はまだ行わない。製品・診断アプリSwift/IPAは6beb877から不変。Tools55件、actionlint、diff check成功。実機依頼・0.8出荷は未完。


9回目の実行は[34819734774](https://github.com/y-aplus/JibunKit/actions/runs/34819734774)、source `b62a2e635993900b47cf3738570b3f1d2825893b`。Webのみ再検証中。network jobを予定から外した際、preflightが通知の未完OS条件P1-4.osの予定欠落を検出した。合格条件を緩めず、候補IPAの通知カード操作を`planned_device_checks`へ明示する。Simulator/deviceの両方を許す条件に対する実機の実施予定はpreflightだけで認め、ci/releaseはpassed evidenceなしでは通らないことをテストした。CI-only条件・空手順・不正source・別checkpoint・重複も拒否する。実機確認は未実施のまま。ローカルTools55件とpreflightが成功し、進行中CIのソースは動かさず別branchへ記録した。

## 8回目：診断IPA生成成功、Web前処理1件が未完

[34816553410](https://github.com/y-aplus/JibunKit/actions/runs/34816553410)、source `6beb87760ffbd0dedd7aab9f98dee3dcde6b8604`はfailure。native job成功、固定8766を含む7方法1.041秒・失敗0。network jobも成功し、HTTP334.018秒、通知foreground424.011秒・添付/取消/管理/B保持480.543秒が成功した。WebはOS認証/取消/A保持203.749秒、保留書込取消119.240秒、管理drain/削除165.790秒、保存/再起動/片側削除248.095秒が成功。

唯一の失敗は無効化試験の本体より前。共通prepareが毎回A削除を行い、60秒後も状態の横に「通知・検索などの登録解除」のActivityIndicatorが残っていた。エラー表示やWebデータ消去の失敗ではなく、native unregisterが未完了。完成状態だけを待つassertionは正しく失敗した。空Aが必須なのは保留書込取消の試験だけなので、その試験のみ明示resetを要求する。他の試験は自身でデータを上書きするか、enable/auth状態を検査する。試験本体の削除・B保持のassertionは維持する。無効化も同じ登録解除を通るため、管理の完了待ち上限を120秒へ揃える。固定sleep、自動再試行、OS処理の省略はしない。

診断IPAは[artifact10337591637](https://github.com/y-aplus/JibunKit/actions/runs/34816553410/artifacts/10337591637)から取得。2,972,156bytes、SHA256 `b815cb203cb356f31943ddb98fb931180c84bc8acf6c3c9a2960be65a430ecd3`がCI packagingログと一致、67 ZIP entriesのCRC正常。CIのbundle/version/署名検査も成功。まだ公開配布・実機確認はしていない。

9回目は`generated_ui_shard=web-management`でWeb5方法だけを実行する。共通setup変更により成功済み4件も対象。全P1 Registry/元filterは変えず、jobだけ選ぶ。変更のないnative7、HTTP/通知3とIPAは再利用する。run8実時間network34.77分/Web30.87分/native3.77分。Webの不要reset4回を除いた再検証は29分見込み、上限45分。製品と診断アプリのSwiftソースは同一で、IPA再生成はしない。Tools54件/actionlint/diff検査成功。

## 7回目：固定portと管理sheetの修正

[34814352424](https://github.com/y-aplus/JibunKit/actions/runs/34814352424)、source `0d255f2b83d72a17da549a0489f73b66e8d5907f`はfailure。全11方法を実行し5成功/6失敗。P0 lifetime33.151/15.376秒と管理失敗/再試行193.062秒、通知foreground208.185秒と添付/取消/管理/B保持541.907秒が成功した。HTTPとWebの4件は起動時のNetwork EINVAL、Web管理drainの1件は試験操作でsheetが消えた。診断IPAは未生成。

取得した`Simulator-text-diagnostics-generated-web-management`のsimulator-app.logに、`nw_listener_copy_parameters_with_port Local endpoint has port set, cannot override to 8766: 127.0.0.1:8766`が4回記録されている。`requiredLocalEndpoint`と`NWListener(using:on:)`の両方に固定portを指定していた。[AppleのSwiftNIO実装](https://github.com/apple/swift-nio-transport-services/blob/main/Sources/NIOTransportServices/StateManagedListenerChannel.swift)と同じく、local endpointへ指定して`NWListener(using:)`を使う。IPv4 loopback/固定8766を維持し、LAN listenerや別portへのfallbackへ変えない。

前回の専用6試験は全てport0で、固定portの生成経路を覆っていなかった。診断版と同じdefault initializerから8766へbindし、実HTTPでcallbackページを取得する7件目を追加した。以前の6成功は履歴として残し、修正後の合格には再利用しない。

管理drain試験のログは、sheetを開いた直後、まだlazy ListにないA行を探して下方向swipeを連続し、management.listが消えたことを示す。共通P1 helperは下の行を先に探索し、見つからない場合に逆方向へ戻る。商品側のsheetを閉じる操作を禁止したり、成功assertionを省いたりはしない。

実装source `8def972df5cc196696deea06aa2d351a8b0ad5b9`。Tools54件/actionlint/diff成功。8回目はhelperを使うP1全8方法と専用7方法を再検証する。P0の3方法は今回の成功を再利用し、診断hostのRegistryにP0 probeを残したまま実行selectorのみ外す。見込みnetwork26分/Web26分/native5分。公開版・製品Coreに変更なし。実機/0.8出荷判断は引き続き未完。

## 最新結果：6回目と7回目の再検証範囲

[34813362141](https://github.com/y-aplus/JibunKit/actions/runs/34813362141)、source `2e0438e76a72228c09a9a17f1eabfb7522e672d7`はfailure。専用端末HTTP jobは3.68分でsuccess、実TCP/URLSession XCTest6件が0.723秒・failure0。artifactを取得し、tests.jsonの6方法とtests.logの個別pass/失敗0を照合した。起動/再起動、実cache/HTML、hold順序/重複、gate上限/期限、不正header/EOF/サイズ/header期限と解放の成功証拠を保持する。

両方のP1-A/P1-B統合Release hostはbuild成功。UI起動stepはmacOS標準Bashの`set -u`で空の`fixture=()`を展開し、`fixture[@]: unbound variable`となった。UIは0件実行、IPA未生成。接続先やSwiftのコンパイル失敗ではない。空のprefixを引数を透過する`env`に変更した。同形のBackgroundTasks用optional引数も、常に非空のcommand配列へまとめた。後者のnative処理自体を今回のP1成功証拠へ含めない。

実workflowからprefix選択部分を抽出してBashで実行する試験を追加し、端末内/外部/HTTPなしの3経路で空白入り引数、環境変数、正常/失敗終了コードを検証した。外部経路は実HTTP serverを使う。ローカルBashはGit付属版であり、macOS Bash 3.2実行の代替合格とは言わない。全Tools54件、actionlint、diff checkが成功。

7回目は未実行11 UIとIPA生成を二jobで再検証する。専用HTTP試験のSwift/Project/runnerは同一なので`p1_device_http_validation=false`で今回の成功証拠を再利用する。この入力のdefaultはtrueで、新しいfixture変更時は通常どおり実行する。製品変更なし。実機・0.8文書監査・正式1.0境界判断は未完。

## 現在の候補：端末単体のP1診断（6回目のCI前）

実装source `e6cfd53935d4c24ac5cedd8070209f9aec7f79c3`。HTTPワーカーの初回/一括レビュー提出を取り込み、親レビューで残った送信前cancel、実効性のないtimeout、待機gateの解放漏れ、停止後logout時のendpoint消失を修正した。新しいNetwork.framework serverは診断hostだけへ入り、端末内127.0.0.1:8766で実HTTPを返す。起動continuationを一度だけ解決し、起動後の失敗も監視する。応答送信完了後に接続を閉じ、header/応答/hold/待機の期限、接続64・gate32・waiter16・header16KiBの上限を持つ。releaseが先でも後でも一度だけhold応答を完了し、取消後の遅いCookie応答を実URLSessionへ返す試験を維持する。

Web認証のendpoint準備はFeature lifetime内に置き、ボタンから独立Taskを起動しない。診断専用build requirementsへcallback schemeとlocal networking ATSを加える。通常版の設定は変更しない。[Appleのlistener port仕様](https://developer.apple.com/documentation/network/nwlistener/port)、[local endpoint指定](https://developer.apple.com/documentation/network/nwparameters/requiredlocalendpoint)、[送信完了とwrite-close](https://developer.apple.com/documentation/network/nwconnection/send(content:contentcontext:iscomplete:completion:))を参照した。

専用iOS-hosted XCTest6件で同時起動/再起動、本文と実URLCache、HTML callbackページ、await-before-hold/重複拒否/release-before-hold、gate容量/期限、壊れたheader/重複/EOF/サイズ/期限の後始末を検証する。実TCPの試験であり、Windows上では未実行。通常hostにもASWebAuthenticationSessionのA/B返却とOS取消後のA保持を1件追加した。ローカルTools53件、actionlint、diff check、delivery preflightは成功。P1-A Intent/Widgetと全P1-Bを同時に組み込むpreparerの合成試験を含む。

6回目は同一のP1-A/P1-B Registryを持つ二jobへ11 UI methodを3件/8件に分け、別jobで上記6件を実行する。network26分、Web+管理30分、専用fixture12分の見込み。network jobから診断IPAも生成する。これは新しい端末内fixture/通常host接続の検証であり、前回cancelledの表示だけを変える再実行ではない。変更していない通常版/Records/Notes/独立native認証の既存証拠を保持する。iOS結果、端末操作、0.8文書監査、正式1.0境界判断は未完で、診断IPAのversionは現行0.7.0/build8を維持する。

2026-09-14着手。契約は[delivery/P1-B-contract.md](../delivery/P1-B-contract.md)、全レーン開始source `e4aec306c23f214f009522d1892cc78041816a9d`。P1-Aの非実機受入をmainへ反映済み。P1-AのOS実機条件と合わせて0.8候補で確認し、1.0正式境界をその時点でユーザーと確定する。

## 実装レーン

親は`codex/p1-b-notifications`で通知・共通統合を担当。HTTPは`codex/p1-b-http`、Codex開発thread `01a09db0-50a4-7f30-ae4a-f99a4f8fc780`。Webは`codex/p1-b-web`、thread `01a09db0-508f-7e93-a0f1-f4fd5f45ff15`。二つとも専用Git worktreeのSol low。開始イベントを各一度確認し、進捗pollをせずOSプロセスの終了通知→codex queueを待つ。Chatタスク/サブエージェントは使っていない。

提出は各work/p1b-submission.md、最終応答の保存先は別ファイル。子CI/push禁止、共有ファイルの変更案を一括して親へ提出する。親は二提出と自身の実装をまとめてレビューし、CI前に共通編集とselectorsを固定する。

HTTP初回は2026-09-14 02:13 UTCにモデル容量不足で終了し、編集はなかった。同じ開発threadをSol lowのまま一度再開したが容量不足が再発。Webの一括修正も同理由で終了したため、両threadをTerra lowへ切り替えて再開し、開始イベントを一度確認した。権限設定の変更や別thread作成はしていない。Webはcommit `b0292cc` を提出済み。一括レビューではJS失敗引数の未渡し、遅延時間依存、古いready状態、操作handleの上書き、launcher到達性と試験独立性を修正対象として返した。CI前の統合は未完。

Web提出で見つかった通常管理の停止順序を修正中。新規受付を閉じた後、Feature所有の処理が保持している通常store accessを停止・drainしてから排他予約する。停止対象外の外部accessが残れば予約競合で削除せず、再試行可能な管理状態を保つ。既存停止待ち試験を実際のstore access保持へ強化し、新規Aアクセス拒否とB利用継続も確認する。Swift実行は統合CI待ち。

## 通知添付の基盤

Apple公式のUNNotificationAttachment/attachment.urlを確認。ローカル添付はOS管理領域へ移されるため、通常Feature原本とは別のowner別・operation UUID別コピーを渡す補助を追加した。native request/content APIは標準のまま利用する。コピーをMainActor外で行い、取消時もコピーの実終了を待つ。operation内でnative add完了までawaitし、OS成功後の遅い取消を失敗へ変換しない。終了時には準備フォルダだけ片付け、OS添付URLや原本には触れない。残った準備cacheのowner別削除は所有writerをdrainした後に行う。

Foundation試験5件を追加：native相当のmoveと原本保持、登録失敗/取消の解放、後続source欠落時の部分コピー掃除、A削除中のBコピー保持、取消してもoperationが実終了するまでコピーを保持。これは実UNUserNotificationCenterの登録/配送証拠ではない。ガイドに接続順序とApple出典を示した。

通常Definitionの通知A/Bペアを追加。任意時刻のnative登録、OS添付のsecurity-scope読込と原本のbyte一致、原本/準備cacheの状態、foreground方針の実delegate callback、通常action/text inputの所有者別記録を表示する。処理は通常store accessとFeature lifetimeに参加。準備コピーを持つ取消待ちは時間経過で成功しないため、管理による停止を試せる。Foundation停止試験も実store access保持と取消後drain/再試行へ強化した。

`P1NotificationsUITests`は予約保持/登録前失敗/取消/再起動/A無効化・削除/B保持とforeground所有者選択の2件。native action/text inputの画面は追加済みだが、OS操作の証拠は未取得。0.8の一括実機確認に含め、表示だけでOS動作を完了扱いしない。

`prepare-p1-b-host.py`で選択ペアのソースをバイト列のままコピーし、普通のRegistryへ一度だけ接続する。欠落ペア、重複適用、既存ファイル保護、別ペア追加、通常targets保持の6件を含むTools unittest全43件がWindowsで成功。変更した2workflowはactionlint 1.7.12で成功、diff checkも成功。native Web認証を上限30分の独立jobへ移し、通常IPA/生成hostとは並行する。

Swift/XCTestは未実行で、P1-B全体のCI前preflightも未完。他レーンのHTTP/Web最終統合、selector/時間の固定、matrix初回実動、0.8実機/文書監査/リリースは残っている。小変更だけでCIを起動しない。

## 提出後の統合レビュー

Webの初回`b0292cc`と一括修正`b759b2b`を親ブランチへ統合（`666b8af` / `69eebfd`）。JS故障引数、所有operation handle、準備済みページの保持、取消可能な書込保留は修正済み。親側でさらにObservation対象外のlazy lifetime指定、保留オブジェクトの操作別所有、通常管理からのA初期化、状態を起点とした再有効化、双方向スクロールと起動ownerの確認を追加。Web保存の4 UI selectorと通知の2 selectorはsource検査成功。実行結果ではない。

HTTPの初回`2abf817`は受領し、まだ統合していない。認証応答のBase64比較、アプリへのport受渡し、書込中logoutと停止後画面、実サーバーの開始/応答判定、異なる候補値による保存失敗の検証、管理項目への到達性、取消と他エラーの区別を一括修正として同じTerra low threadへ返した。共有Workflowではloopback serverを生成UI jobに限定して起動し、test runnerへの`SIMCTL_CHILD_`環境と通常環境を設定、shell終了時にfixtureを停止する。アプリ自身へのlaunchEnvironment接続は担当の修正対象。

native Web認証はfixture内の全test methodを列挙し、それぞれに一度のpassedログがなければjobを失敗にする。全件成功時だけ`WebAuthentication-tests.json`を証拠artifactへ出す。独立job上限30分は維持。生成hostは既存の各selector/log検査を利用する。

次のCIは通常/生成matrixとnative Web認証を一つのrunで起動する予定。通常29分、生成はP1-Aの23分から旧P1 UI約3分を置き換え、新P1-B UI約10分を見込んで30分を目安とする。これは新UIの未実測見込みであり、HTTP最終提出後にselectorsと件数を確定してpreflightへ記録する。45分timeoutへの到達や見込み超過があれば直列追加を続けず、失敗箇所・経過時間をもとに境界を見直す。通知action/文字入力と実機更新条件は別途証拠が必要で、CIの2通知試験だけで0.8受入を完了しない。

## HTTP最終統合と停止後操作

HTTPの一括修正版`6d497eb`を親へ統合（`e9cf3b5`）。終了した担当threadは追加起動していない。認証の実応答、異なる候補Cookie/password、server側開始応答、HTTPエラーと取消の区別、UIへのport伝播、管理状態の確認を採用した。

提出でも残った停止後logoutと再起動/管理削除の競合は、Core `MiniAppFeatureLifetime.withStoppedOperation`で補う。runtimeの全処理をdrainし、その後の外部操作の実終了までstart/stop/管理を待たせる。管理の受付状態は変更しない。外部操作は通常のexclusive store maintenanceへ参加し、同じlifetimeのstart/stop等を内部から呼んではならない。caller取消でも操作の実終了まで境界を保持し、終了後の取消を一律に成功済みcommitの失敗へ変換しない。

Foundation試験2件で、writer取消と実終了の差、停止後cleanup中の再起動待ち、重複cleanup拒否、B継続、取消後も管理削除を待たせること、removed ownerの再起動拒否を検証する。Swift実行はCI待ち。HTTPの取消検証自体はruntime所有Taskにし、ログアウトだけ外部停止操作として接続。キャッシュ読込は専用操作に分け、再起動/削除後にOS cacheの保持を必須にしない。候補失敗後はURLSessionも再構築して記憶済み認証を捨てる。Observationの複数変数宣言も分割した。

今回Coreの停止/再起動境界を変えたため、共有unit全体、通常UI、生成P0 lifetimeの2件と管理失敗1件もP1-Bの7 UIに併せて確認する。生成jobは30分程度の見込みで、増分は未実測。P1-B初回結果で実績を測り、45分上限まで消耗する前提を常態化しない。これらとnative Web認証5件を一つのCI境界として扱う。

## 初回CI実行中

2026-09-14 03:03 UTCに[34801232847](https://github.com/y-aplus/JibunKit/actions/runs/34801232847)をsource `5538dd96087093f57801b44b4fffae57e933dd4a`でdispatch。事前検査は構造/対象網羅成功、10 selectorの存在確認成功。完了通知はOS監視からqueueへ一回送り、モデルの定期pollはしない。

dispatch後のsource確認でHTTP診断の空文字表示を`(empty)`へ変更し、対応UI期待値も揃えた。空文字のTextがアクセシビリティ要素にならない場合の到達性問題を避ける変更で、実行中CIには未反映。実行中のCore/通常IPA/Web等の検証は引き続き有効な対象なので、小さな診断表示変更だけでrunを取消/再dispatchしない。結果確認時にはこの差分を区別し、HTTPの実行結果を後続sourceの成功へ自動転用しない。

## 初回CIの失敗と2回目の修正

34801232847はfailure。normalは新しい停止操作試験のSendable removal closureからMainActorの値を直接変更したコンパイルエラー。generatedは通知removal callbackのawait不足、通知/WebのVoid throwing continuationの型推論不足でコンパイル失敗。MainActorへの明示的な受渡しと`CheckedContinuation<Void, Error>`を指定した。normalのunit/IPA/UIと生成host UIは未実行。生成工程のFeature build requirements、Records単独Release、生成Featureの11 package testsは成功したが、P1-B受入へ拡大しない。

native Web認証は5件中4件成功。wrapper取消20.056秒、Apple直接callback17.688秒、直接取消15.730秒、A/B callback50.273秒。最初のwrapper callbackはページの「Return to App」が10秒内に見つからず失敗（試験全体92.208秒、うち失敗診断取得にも時間を要した）。後続の同じwrapperを使うA/B callbackが成功したので常時失敗ではないが、初回起動の準備不足と断定できる画面証拠はない。ページ内容を最大45秒待ち、初期確認後に現れたContinueも処理する。失敗時にはapp/system両方の画面とUI階層を保存し、native jobでxcresultからattachmentsを展開して保存する。固定sleepやアプリ動作の成功置換は入れない。

修正後のTools43試験、actionlint、diff checkは成功。Core/hostのコンパイル修正、dispatch後の空値表示、native認証の診断改善をまとめ、同じ10生成selectorと5 native試験、通常回帰を2回目で実行する。初回の時間はコンパイル段階で止まったため、正常時の30分見込みを実証したものではない。

## 2回目の結果と残りをまとめた再検証

[34802338245](https://github.com/y-aplus/JibunKit/actions/runs/34802338245)、source `8b5b8b8bf41d9973f0f3733d1e2f1ba7fbaf21ee`はfailure。normal jobは33.82分で成功。共有270件（既知skip 2件、failure 0）、新しい停止後操作2件・添付準備5件、独立package、通常UI13件、Files往復167.066秒が成功した。IPAのnative検査/署名/package/uploadも成功。ダウンロードしたIPAは2,226,509 bytes、SHA256 `15cad6ebff7d66b53fdd192f88d3b59561f61dd6fb4d72fb9fddd37a0bc7c0f2`、ZIP CRC全件正常。新しい実機証拠ではない。

生成jobではFeature build requirements、Records Release、生成host/Feature Release、Notes単独UIが成功。P1 host UIは開始前にHTTPサーバーのport fileを待つ約5秒の試行を使い切り、無診断でexit 1になった。正確なPython起動遅延の内訳はログにない。port fileをshellでpollする経路を除き、既存fixtureの`--run-tests`同様、Python自身がbind/serveを準備してから`--run-command`の子コマンドを起動する形へ変更した。成功・失敗終了とserver解放、実HTTP応答をWindowsのsubprocess試験2件で確認。XCTest runnerへ渡すportも[Appleの環境変数仕様](https://developer.apple.com/documentation/xcode/environment-variable-reference)に従い`TEST_RUNNER_`を付け、そこからアプリへ明示的に渡す。

Records編集/永続化は成功。添付試験はexport直後に「保存」が見つからず失敗。artifactのUI階層ではFilesが「ブラウズ」の場所一覧におり、`DOC.sidebar.item.このiPhone内`が存在し、保存先未選択だった。試験を、場所一覧なら通常操作でローカル保存先を選んでから保存する手順へ修正した。保存先を別の試験が事前に開いていることには依存しない。Recordsの製品実装は変更していない。

native Web jobは診断追加部分でコンパイル失敗。IUOのappとsystemを配列化した際にOptionalへ推論され、screenshotを呼べなかった。appを非Optionalの固定インスタンスに変更した。このrunで認証UIは実行されておらず、初回ページ表示の問題が解消したとは扱わない。

3回目は`generated_validation_only=true`を使い、成功済みの通常版・Feature build requirements・Notes単独UIを再利用する。変更は試験起動/診断/Records操作とCIだけで、Core/通常Feature/Notes/生成targetsは同一。生成hostは再生成・buildし、予定の10件とRecords build/UI、別jobのnative認証5件を実行する。生成job実績26分から省略可能なrequirements 4.55分とNotes UI 4.88分を引き、未実測の統合UI約10分を足して27分を見込む。上限45分を維持し、見込み超過なら次の境界でjobを分ける。通常jobの33.82分は以前の29分見込みを超えており、次の全体境界へ旧見込みを流用しない。

修正後のTools全45試験、actionlint、preflight網羅検査、diff checkが成功。P1 HTTP/Web/通知の通常host動作、native認証全件、通知action/文字入力と0.8一括実機/文書監査/正式v1境界判断は引き続き未完。

## 3回目の結果

[34805047488](https://github.com/y-aplus/JibunKit/actions/runs/34805047488)、source `02af62cd56179e1646ce655f4c16edf5c5e6d48a`はfailure。native Web認証jobは15.97分で成功し、artifactのJSONと実ログの全5 passを照合した。wrapper callback 88.266秒、wrapper OS取消19.417秒、直接callback 21.632秒、直接OS取消21.114秒、A/B callback36.737秒。初回callbackを含めて成功したが、実機での新しい証拠ではない。

HTTP fixtureの起動は成功し、`Loopback HTTP fixture ready on 50113; starting xcodebuild`を確認。生成host UIのコンパイルまで初めて到達したが、`P1WebUITests.setUp()`がXCTest由来のnonisolated overrideで、MainActorの管理操作helperへselfを渡してコンパイル失敗。HTTP/通知と同じく、MainActorの各testから明示的に`prepare()`を呼ぶ形に揃えた。他の生成UI setup overrideも確認し、同様のUI helper呼出はなかった。10件のhost UIは未実行。

Recordsの編集/永続化は成功。添付試験は場所Cellの`tap()`が自動scrollを試みる間にCellが消えた。失敗artifactでは既に「このiPhone内は空です」と保存用filename欄が表示され、Filesがローカル保存先へ移行済みだった。前回の「保存先を必ず選ぶ必要がある」という推測だけでは不十分で、場所一覧が遷移途中の状態でもあると判明。操作可能な保存ボタンを優先し、操作可能な場所一覧が残る場合だけ表示中frameを通常タップする。消えかけのremote CellをXCTestへscrollさせない。画面操作後の保存/取込み/内容一致/添付保持/削除のassertionは維持する。

生成jobは17.60分で終了したが、P1 UIの実測はまだない。4回目は上記2試験の修正をまとめ、10 selectorとRecordsをすべて実行する。通常版等は2回目、native Web認証は3回目の成功をsource差分確認の上で再利用する。予定3回を超える理由と範囲縮小をevidenceのbudget_exceptionへ記録した。製品変更なしで成功済みnative jobをもう一度回すことはしない。P1-B全体の完了ではない。

## 4回目の結果と共通UI操作の修正

[34806399426](https://github.com/y-aplus/JibunKit/actions/runs/34806399426)、source `e15dec98cc8622e52e3395ecbd72e57d5027aa15`はfailure。生成job28.65分、全10 UI methodを実行し5成功/5失敗、UI合計590.107秒。Records添付の取込み・保持・削除105.990秒と編集/永続化71.428秒はともに成功。既知のSimulator Quick Look expected failureはこの成功から除外せず、従来どおり制限付き証拠として保持する。

P0 lifetime2件（36.230/14.474秒）が成功。Webの無効化拒否68.098秒、保留書込取消47.255秒、管理削除によるdrain/B保持82.799秒も成功した。HTTPは同一originのA/B認証/Cookie、Aのcache読込、process再起動、異なる候補保存失敗から旧状態保持まで進んだ。これらは未完methodの途中のassertionであり、HTTP全体の合格とはしない。

HTTP/通知/Webの残る失敗は、操作後の状態が直前のまま。失敗時階層でHTTPボタンはy849.7〜914、通知の読込はy842.3〜894.6、Webの読込はy821.7〜874にあり、画面下端874に対して一部が見切れ/安全領域外だった。`isHittable`だけでは押下位置を保証できなかった。P0の既存位置調整をもとに、Listとnavigation/search barの内側へ対象全体を移す`P1UIVisibility`を3 suiteで共有する。固定toolbarとListを持たない停止後再開画面は別に扱う。操作を成功扱いする代替や自動再試行は入れず、既存の実データassertionを維持する。

P0管理再試行の失敗時階層は既に「削除済み」だった。native通知/Spotlight解除を含む完了を15秒で打ち切っていたため、他の管理試験と同じ60秒上限へ揃える。削除中を完了と扱う変更ではない。製品コードは変更していない。

helperの正確なコピーと追加入力時の保持、欠落・競合時の部分変更拒否をToolsで検査し、全47試験が成功。actionlint、selector存在とpreflightの網羅も確認する。5回目は全10 UIをまとめて実行し、helperを変更したため成功済みWeb3件も再実行する。Recordsは今回の成功と変更なしを根拠に`records_validation=false`で省略する。通常版とnative認証も従来の成功証拠を保持する。追加CIの理由・scope・時間見込みをevidenceへ記録した。

## 5回目：全10試験成功、runは時間上限でcancelled

[34808525786](https://github.com/y-aplus/JibunKit/actions/runs/34808525786)、source `f6e82dfa7e1007062b1e79f4f66576a87f30bfb1`。全10 methodがsuccess、1981.667秒、failure0、`TEST SUCCEEDED`と全10 selector validatorの成功を確認した。試験/出力/uploadを含む全stepがsuccessだが、GitHub annotationは45分の最大時間超過、runはcancelled。46.53分かかっており、実行全体をsuccessへ読み替えない。

P0 lifetime92.459/18.931秒、管理再試行238.521秒、HTTP315.422秒、通知2件608.658秒、Web4件707.676秒。HTTPは認証/Cookie/cacheから再起動、故障保持、401、取消、停止後logout、削除/再登録/B保持まで完了。通知は原本/OS添付/予約保持、取消/失敗、管理停止/削除/B保持、実foreground方針を確認。Webは再起動保持・故障・取消・管理削除の4件が完了。OS通知カードのaction/text inputと配信済みB通知は引き続き未確認。

[artifact10334184131](https://github.com/y-aplus/JibunKit/actions/runs/34808525786/artifacts/10334184131)（253,174,857 bytes）をダウンロードし、保存されたtemplate-host.logも10成功/failure0と照合した。xcresult/画面証拠を保持。upload時のartifact SHA256は`4eb40d6504d1c44ed813ee09746e014db35ca92350bd0e3ebb61d3f767e732c2`。成功証拠を捨てて同じrunを再実行しない。

次の必要なCIは同一run内の二jobに分ける。HTTP+通知の実測15.40分とWeb+管理の17.63分に分配し、同一の全Feature Registryを作り、実行methodだけを分ける。準備/export込み24/27分を見込む。50 Tools試験（分配の全件網羅/重複なし/空入力拒否を含む）、actionlintは成功。新しい分配は次の候補変更とまとめてCI実行する。

0.8の実機準備にはPC外部serverなしのHTTP/認証endpointが必要。既存HTTP開発threadをTerra lowで再開し、clean worktreeを`codex/p1-device-http`（48ae2a6）へ切替。診断専用loopback serverとHTTP/Web probe接続だけを担当し、Core/Registry/workflowを触らず、子CI/push禁止。親は証拠/台帳/CI分配を担当。開始イベントを一度確認し、提出はOS wrapperからqueueへ一回通知する。現時点でその実装は未提出・未検証であり、既存の成功証拠へ混ぜない。
