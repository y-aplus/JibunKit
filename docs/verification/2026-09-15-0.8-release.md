# 0.8.0出荷の照合

現在: 0.8.0を公開済み。CI・対象別実機・P0/P1証拠照合・現況文書58件のレビュー・構造gate・公開IPA/ZIP再取得を完了。以下の未完表現は各進捗時点の記録であり、最終結果は末尾へ追記する。

候補準備開始時の最新安定版は0.7.0だった。0.7.1/build9の4e6a3f4診断候補でP1再確認を受領し、0.8.0/build10候補へ進む。0.7.1の正式releaseは作らず、過去の診断prereleaseを保持する。1.0の正式範囲は0.8.0完了時のユーザー判断のまま。

## 実機と通常IPA

[実機の正本](2026-09-14-0.8-device-check.md)に操作別の結果と未観測の範囲を保存。共有/Files、Shortcuts、Widget、更新/Refresh、通常版復帰を受領済み。以前に成功したHTTP/Web/通知は元sourceを保持して再利用する。旧表示が残る診断Widgetは、通常IPAに診断kind/resourceがないことと別の観測である。Widget旧表示から診断データの削除や再読込は判定しない。

## CI境界（投入前固定）

追加runは1回。`build-ios.yml`で`simulator_tests=false, feature_validation=false, records_validation=false, simulator_runtime=''`、残る入力は既定。通常Release app/Widget/Share、共通試験/独立Records、署名/metadata/IPA/版を検査する。直近同条件34958710691は5分39秒。今回のrun全体は10分見込み、30分以内を目標とする。既存workflowの安全上限は延長しない。失敗すれば最初の不一致で切り分け、同じ再試行を3回超繰り返さない。

4e6a3f4から製品挙動・保存形式・ID・Registry・entitlementに変更なし。Project.swiftは3bundleの版0.8.0/build10のみ変更、build-iosの期待値を同期する。別途準備済みのnative-surface iOS27選択と3構成試験は通常runから呼ばない。通常IPAと今回の版検査は新runで行い、旧IPA digestは流用しない。

再利用: 4e6a3f4の34958710691共通273(skip2)/Records11/通常build、34958712557 native32/完全host共有・再試行・B保持。通常UI13/Files JSON復元と更新galleryは8c54d6eの34883605285、Intent管理は340e131の34887580504。通常UIから変わったincoming provider/storeの経路は上記4e6a3f4で補完済み。P0/P1-A/P1-Bの残るsource別証拠は既存evidenceから最終reportへ照合する。実機成功を版更新後の未取得IPAそのものの実機試験と呼ばない。

## 未完の出荷gate

- 新候補CIと通常IPAの取得/CRC/3bundle版・ID・診断fixture非混入。
- P0/P1の全条件と既存source別証拠の最終照合。
- minor現況文書全件の読了・同期、確認commitと一覧/まとめ/未解決事項のreport作成。
- main統合、固定source/tag、通常IPA公開と認証なし再取得、公開後文章同期。

現時点で公開済み・P1全単位completeとは記載しない。実機確認を追加要求する新しい理由はない。

## 候補固定と確認の進捗

候補sourceは`71ef1ff`（0.8.0/build10）。[CI34967147135](https://github.com/y-aplus/JibunKit/actions/runs/34967147135)を上記入力で1回投入し、OS側の完了監視と同じ会話へのqueue通知を登録済み。モデルによる定期確認はしない。

文書レビューは継続中。README/CONTRIBUTING/SECURITY/THIRD_PARTY_NOTICES/Records README/build/SideStore/updatingの本文を読み、READMEの旧診断リンクと実機待ち、buildの「今回」の参照、SideStoreの実機未確認表現を修正。診断Widget旧表示の扱いも追加した。全58件の完了扱いではなく、最終reportの確認commit・全件一覧は全体読了後に作る。

## CI・通常IPA確認

34967147135はheadSha `71ef1ffb4f84442bf8853c0c2e286c2bedd81d22`、5分16秒でsuccess。共通273件（skip2、失敗0）、Records11件（失敗0）、通常Release/metadata/署名/IPA検査成功。IPAを取得し全entry CRC、app/Widget/Shareの0.8.0/build10・既存3bundle ID、Counter Widget kindあり・診断A/B kind/resourceなしを確認。IPAは2,243,242 bytes、SHA-256 `50626937264c8cb4cef7193f1c3035e06e1dc1cc1a5d6d1faf2f3fbd80cf6ff6`。Simulatorを本runで再実行したとは記載しない。

文書残件をSol lowへ一括委任したが、JibunKitに紐づかないprojectless作成だったためユーザー指摘を受け、編集停止と途中結果の提出を依頼した。最終監査は親が引き取る。子CIは起動していない。新たな承認をユーザーへ要求して出荷作業を止める運用にはしない。


## 最終のsource・合格条件レビュー

実装/試験の新規変更はない。71ef1ffがIPA source、以後は文書/証拠だけの差分である。全条件の対応は[集約evidence](2026-09-15-0.8-evidence.json)を正本とする。

| 条件群 | 再利用と今回の照合 |
| --- | --- |
| P0-1/3/5/6 unit | 71ef1ff/34967147135の共有273件（skip2）を使用。UI成功をこのrunへ転記しない |
| P0提示/UIKit、Records/SQLite保守 | 7906bd9/34746211458の対象実行を維持。P1追加はoptional incomingと停止後操作で、既存保存形式/提示/onboardingは不変。新しい寿命/管理は後続対象CIでも成功 |
| P0寿命/管理UI | 0d255f2/34814352424の対象methodを維持。後続6beb877からのRegistry差分はDEBUG診断のみ。0a3fb21から71ef1ffのSources差分はincoming provider/storeとShare controllerのみ |
| P0追加/更新/復旧 | baa041f/34705653297のnative診断6ケース、Registry漏れ/回復、同じiOS hostのAだけ互換更新/B保存・resource保持。template/対象Package/validatorの契約不変を照合 |
| 通常回帰 | 8c54d6e/34883605285の通常UI13件・Files JSON1件。後続で変えたincomingは4e6a3f4のnative32件・完全host共有/再試行と実機で補完 |
| P1 Intents | 6dd95cc/34881183837のnative8件+管理UI1件と独立/統合metadata。Tests/PackageAppIntentsと既存Counter Intent/Shortcutは同source以後変更なし。完全host管理UIは340e131/34887580504 |
| P1 Widget/共有実機 | 4e6a3f4の操作別実機結果を使用。旧6beb877のgallery非表示、09927a6のinvalidInputは失敗履歴のまま保持 |
| P1 HTTP/通知/Web | 6beb877の通常host HTTP/通知、b62a2e6のWeb保存/取消/drain、0a3fb21の認証/無効化UI。対応する製品/fixtureは最終候補でも不変。6beb877実機のHTTP/Web/通知を4e6a3f4実測へ付け替えない |
| P0/P1端末更新 | P0はbaa041fの旧実機証拠、HTTP/Webは6beb877の上書き/Refresh、Widget/受信/通常復帰は4e6a3f4。71ef1ffは版のみの製品差分で新しい実機操作を要求しない |

失敗直後のinbox行は実機で独立観測していないが、完全host CIに失敗/行保持/再試行のassertionがあり、実機は同手順の最終重複なし・B保持・再起動保持を確認した。Widgetは上書き/Refresh後の起動保持を受領し、単純な再起動だけの再試験とは書かない。この証拠の組合せでP1の合意条件を満たすと判断した。OSの即時更新や診断コード除去後のホーム配置削除まで合格範囲を広げない。

## 現況文書58件の全件監査

親が対象本文を読み直し、先行監査の12件を含む全58件を確認した。中止した文書担当の7ファイル差分を回収したが、そのまま採用せずHTTP/Web/通知と通常Shortcutsのsource誤記、5秒の旧診断手順等を修正した。担当はcommit/CIなしで終了。projectlessへの代替作成を繰り返さないよう並列運用へ明記した。

変更した文書群: P1のガイド、mini-apps/network、台帳/計画/status、公開/導入の版区分とrelease notes。古い実機待ちを結果へ同期し、観測していない細部や古い候補での成功を区別した。statusの逐次操作記録は正本の実機/CI記録への参照へ整理した。履歴の失敗/取消や旧tag内の文書は変更していない。

非変更の文書群: P0のAPI/接続/障害回復、保存・scene・SDK・backgroundの利用ガイド、貢献/security/notice等は今回のP1修正で契約が変わらず、本文の既存APIと未対応範囲を確認した。全外部リンクや第三者仕様の最新版再調査、全領域の新規実行試験を行ったという意味ではない。

確認commitと各58pathのupdated/reviewed-unchanged一覧は集約evidenceへ記録する。主な残存事項はP2/P3と1.0境界決定、旧Widget表示/Spotlight遅延原因、集中モード下等の未観測範囲であり、現在状態の文書誤記を残件欄へ逃がしていない。

## 並列・CI運用の振り返り

P0-Aは予定2に対し5run（成功2/失敗3）、担当ごとのCIは0、各担当への一括修正1往復。P1-Aは5run・195.33 job分・レビュー2往復/親メッセージ4。P1-Bから出荷候補までは22run・590.47 job分、実装レビュー3往復/親メッセージ7に加え、文書担当の中止・部分差分回収が1件ある。P1-AとP1-Bのrun集合は別なので、P1全体は27run、785.80 job分である。

CI削減の全体目標を達成したとは言えない。型/隔離と試験入口の不一致、長いUI直列実行、native状態・実機で見つかった入力不具合の再検証が増加要因だった。失敗を残して必要範囲を再実行し、成功済み範囲はsource照合して再利用した。最終の版更新境界は計画1run・実績1run、経過5分16秒で完了。文書整理は追加CIなしで閉じる。

モデル課金/トークンの実測がないため節約率は不明。今回の文書委任は作成場所の不備で親の回収作業を増やしており、成功した分担に数えない。以後は保存済みJibunKitプロジェクトのCodexタスクを使い、契約/提出一式を最初に渡す運用を維持する。

## 公開前の確定検査

現況文書58件の確認commitは`a98dea8ddb7b256fda0800b9ce1b33e8a1a04f93`。前回全件確認7951578から28件更新・30件確認非更新。全pathの結果は出荷evidenceのdocumentsに記録した。`check-delivery.py --stage release --release 0.8.0`は成功。現在文書のローカルリンク先欠落0、追跡path/内容のpublication boundary検査も成功した。製品source71ef1ff以後は文書・証拠のみで、追加CIは行わない。

## 公開結果（2026-09-15 12:53:45 UTC）

[0.8.0](https://github.com/y-aplus/JibunKit/releases/tag/0.8.0)を通常release（draft=false、prerelease=false）として公開した。tag commitは`f5c74e631b71cf59b6665ab447a9c0bd6593031e`、文書確認commitはa98dea8、IPA製品sourceは71ef1ff。mainへfast-forward push済みで、元main作業treeの未commit変更・ignored Zaikoは触れていない。tagは移動しない。

| 公開asset | bytes | SHA-256 |
| --- | ---: | --- |
| [JibunKit.ipa](https://github.com/y-aplus/JibunKit/releases/download/0.8.0/JibunKit.ipa) | 2243242 | `50626937264c8cb4cef7193f1c3035e06e1dc1cc1a5d6d1faf2f3fbd80cf6ff6` |
| [JibunKit-0.8.0.zip](https://github.com/y-aplus/JibunKit/releases/download/0.8.0/JibunKit-0.8.0.zip) | 2217566 | `378aa2d66a30140f57ce96bd13c80b7ee0f68d8f92011287ca53568b0ddcb59b` |

両assetを認証なしの公開URLから再取得し、CIから取得した元IPA/作成ZIPとの全byte一致、GitHub asset digest一致、全entry CRC、外側ZIP内IPAと直リンクIPAの一致を確認した。公開後mainのREADME/CHANGELOG/状態・台帳・計画・互換性/導入更新・P1ガイドを公開済みへ同期した。公開前の古い候補表現は今回の同期で解消し、過去sourceの証拠・過去release notes・既存tagは維持する。

0.8.0完了につき、Issue #6を用いた正式1.0境界の確認をユーザーへ依頼した。回答前にP2範囲を確定・実装開始しない。

公開後の文書確認commitは`0e1e13d2c0d1c38c21a1f733041aee06e92a20d1`。全58件の一覧を更新し、公開後release gateも成功（前回minor確認から30件更新・28件確認非更新）。Issue #5のP1全6単位と0.8.0公開を完了へ更新した。1.0境界への回答は次の計画改定で記録する。
