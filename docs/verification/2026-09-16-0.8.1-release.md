# 0.8.1出荷の照合

現在: 0.8.1/build11の実機・出荷CI・取得IPA検査と関連文書確認を完了。公開前の最新安定版は0.8.0/build10。次にmain統合・正式公開・公開物再取得を行う。以下の準備/待機表現は履歴。

## 範囲と再利用

P2-Wの操作Widget/Control、owner別process共有状態、通常管理/復元の外部受付を一まとまりとして出荷する。通常IPAにはCounter/ReminderとShareを維持し、診断A/Bを含めない。1.0は未達。

実機はe984d44の診断IPAと4eb784dの通常IPA（ともに0.8.0/build10）。[操作別の確認記録](2026-09-15-p2-widget-control-device.md)を正本とする。OSの設定/加算・再起動/上書き/Refresh・片側削除/無効化/復元と他方保持、通常復帰が成功。実機の無効化はほぼ即時、Simulatorでは88秒とSpotlight接続中断があった。原因を完全に解消したとはしない。

35027469173のnative4件/通常hostUI1件/独立・統合metadata、34979381516の通常UI13件/Files往復1件・共通290件/Records11件/別process probeを再利用する。実機確認済みe984d44以後の製品変更はProject.swiftの3bundle版とbuild番号、CI/診断検証の期待版のみ。保存形式・ID・署名条件・Feature接続・実行コードは変更しない。新IPAのCRC/metadata/署名と版は下記runで検査する。

## CI境界（投入前固定）

追加1run、build-ios.yml、simulator_tests=false、feature_validation=false、records_validation=false、simulator_runtime空、他は既定。通常Release app/Widget/Share、共通/独立Package試験、署名・metadata・IPA版を実行する。直近の同範囲0.8.0版変更run34967147135は5分16秒。SharedState17件増分を含め見込み10分、30分以内目標。全UI/nativeの再実行はせずsource差分を記録して再利用する。失敗は最初の不一致で切り分ける。

これはP2-Wの修正再試行でなく、実機確認後の版更新に必要な5回目の出荷CIである。初回2run予算超過はP2-W reportへ累積記録する。モデルの定期pollやinterval付きwatchは使わない。

## 文書・公開の残件

関係する状態一覧、台帳、計画、接続/管理ガイド、README、CHANGELOG、互換/更新/build/公開手順と版別notesを同期する。patchなのでminor全件再監査を実施済みとは主張しない。既存check-deliveryのrelease modeは0.7/0.8/1.0のmilestone用で0.8.1を受け付けないため、P2-Wのci条件検査とこのpatch出荷チェックを使う。gateを通したように記載しない。

新CI成功、取得IPA検査、変更path/証拠/文書照合の後にmainへ統合し、新規0.8.1 tag/正式releaseを公開。公開assetの無認証再取得・完全一致/CRC、公開後の文書同期まで行う。既存tag/assetの差替えとignored Zaikoの変更はしない。

候補sourceは`88ab7cbe9f41a7eb102fed9444923c8ff8812bfd`へ固定。版・検査値だけの差分をレビューし、関連local25件、workflow YAML parse、diff checkが成功。

[CI35038442208](https://github.com/y-aplus/JibunKit/actions/runs/35038442208)のheadを88ab7cbと照合し、OS監視を登録。P2-W ci構造検査は初回にP0-4.docsの記録不足を検出したため、関連するREADME/追加導線/更新/互換/管理ガイドを確認して証拠を追加した。製品試験の失敗や全minor監査完了とは扱わない。

## 出荷CI・取得IPAと最終照合

CI35038442208はsource88ab7cbe9f41a7eb102fed9444923c8ff8812bfd、全体6分18秒/通常job6分07秒で成功。共有290件（skip2・失敗0）、SharedState17件、Records11件、通常Release/署名/metadata/IPA版が成功。Simulator/native診断はこのrunでは再実行していない。e984d44と88ab7cbのSources/Package/Tuist/Tests/UITests/Modulesに差分なし。後続7157d1bまでは文書のみ。旧UI/native/実機証拠の再利用範囲と一致する。

IPAは2,351,004 bytes、SHA-256 `6498605839402ae79c3dbc41395abd650e60d3b1c2d10a7303f20311ca88b987`。取得後に全entry CRC、本体/Widget/Shareの既存3ID・0.8.1/build11、通常Counter Widget kind、新診断kind/Zaiko非混入を確認。単一IPA入りZIPをZIP64なしで作り、内側byte一致と外側CRCも確認した。公開後の取得検査はまだ別工程。

文書確認snapshotは7157d1bc624906531a6a08f0b753ebbcdef2bc16。README、CHANGELOG、status、build、mini-apps、compatibility、updating、releasing、coexistence-ledger、plan.json、interactive-widgets/feature-managementガイド、新版notes、実機記録とP2/出荷reportを対象に、公開前0.8.0と候補0.8.1を分け、実機未確認表現・残件・Control編集操作・sourceを同期した。変更Markdown13件の相対リンクを確認した。版だけ変更したsourceの関連local25件とP2-W ci構造/網羅gateが成功。これはpatchの影響範囲レビューで、minor全件再監査や0.8.1に未対応のrelease-mode合格を装わない。

P2-Wは5run・合計102.833 job分。native修正のCLI sol/low委任は1回提出・追加レビュー往復なし・子CIなし。親はhost修正/統合/出荷を進行した。初回import不足と既知のcold cleanup待機条件の見落としによる失敗2runは改善余地があり、モデル費用の節約率は未計測なので断定しない。次はP2-Lを大きな共通契約から始める。
