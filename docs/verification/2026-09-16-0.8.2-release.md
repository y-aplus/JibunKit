# 0.8.2出荷候補

現在: VERSION 0.8.2/build12、PREVIOUS 公開済み0.8.1/build11。通常版復帰まで実機の一括操作確認を受領。候補source31478f2へ版更新をcommit/pushし、出荷CI35075825942成功、取得IPA検査も完了。main統合・公開準備中。検証負担の監査指摘を受け、ユーザーは保存値/管理/復元の組合せを自動化し、実機をOS固有動作と代表例へ絞る分担を承認した。

## 範囲と未解決事項

Live Activities/AlarmKitの型付きadapter、owner別journal/受付調停、native再照合、通常管理/復元へのoptional接続と独立Feature例を含む。通常IPAはCounter/Reminder/Shareのままで診断Featureを含めない。1.0未達。

実機sourceは診断5678a41、通常b1d379b（双方0.8.1/build11）。[実機記録](2026-09-16-p2-continuing-surfaces-device.md)にOS操作・Alarm標準stop callback・片側管理/復元・cold復帰・上書き/Refresh/端末再起動・通常IPA復帰を記録した。

Live Bが上書き前に期待230ではなく200と表示された観測は原因未特定。200は保存値表示であり、初期の仮表示ではない。210へ更新後の再起動、Alarm A削除/無効化/復元では再現しなかった。操作を忘れた可能性はユーザーの推測で、誤操作と断定しない。P2-5はpartialを維持し、最終保持判定を留保する。確認済み機能の中間公開と、1.0合格判定を分ける。

## 検証の再利用と追加範囲

実機確認済みsourceからの製品差分は3bundleの版/build番号だけ。実行コード・保存形式・ID・App Group・署名条件は変更しない。native/管理UI/実機を再実行する予定はない。通常35046699862、Alarm35046697413、Live/host35047576529の証拠を元sourceのまま参照する。

今回の追加境界は1run・独立2job。build-ios.ymlをcontinuing_state_validation=true、simulator_tests=false、feature_validation=false、records_validation=false、simulator_runtime空（他は既定）で実行する。

- 通常job: 0.8.2/build12のRelease/署名/metadata/IPA。従来workflow内の短い共有・Records・要求合成検査はそのまま使い、Simulator全回帰と独立Featureの再buildは行わない。直近同範囲35046699862は3分53秒、準備/upload込み10分見込み。
- continuing-state job: 実4Featureのstore/provider、公開管理・復元APIを接続する新規native XCTest1件だけ。4ownerへ非初期値を保存し、Live A/Alarm Aの無効化/再有効化、誤JSON拒否、選択JSON復元、削除/再登録、再prepareについて他3ownerの全業務payloadと世代の不変を検査。OS活動は開始せず、OS表示/鳴動の検査とはしない。Registry固有のSpotlight/通知cleanupは既存UIで検証済みのため重複実行しない。

state jobは通常診断hostのSimulator Debug buildと1件実行のみで、Release/独立A/B/Combined/metadata比較/既存UI/診断IPA作成を繰り返さない。既存host16分26秒からRelease buildとUI操作を除外し、新しいnative test targetのcompile/runを追加するため、準備/upload込み15分を見込む（新経路の実績ではない）。通常jobとは並列・依存なし、run最長見込み15分、30分目標。P2-L累計8run（7runは既存実績）。追加理由は実機後の版更新と、承認済みの手動検証削減を支える実Feature接続試験の補強で、実装の細分化による別々の投入はしない。

新試験が成功しても単発のB=200の原因解決とはしない。追加実機は依頼せず、保存値追跡の自動回帰結果として記録する。実機前に候補採番して再buildをなくす案はまだ提案であり、この承認で採番手順まで変えない。

影響のあるREADME/状態/台帳/計画/接続ガイド/互換/更新/build/公開手順/CHANGELOG/版別notesを同期する。patchのためminor全件レビューの実施は主張しない。公開前はPREVIOUSと候補を明確に分け、公開後に状態表記を同期する。既存tag/assetを変更せず、ignored Zaikoを保持する。

## 候補固定と投入

候補`31478f2da9f4262e36e84b6ec4e4c9c1b1685c78`をcodex/p2-l-082-ciへ固定し、[CI35075825942](https://github.com/y-aplus/JibunKit/actions/runs/35075825942)のheadShaと一致確認。完了時に一回通知するOS監視を登録した。実機記録と文書、版変更、診断限定test target/新規状態試験/限定CI入力を一括commitし、Sources本体は変更していない。Tools関連24件、実追跡ソースからの診断host生成、YAML parse、変更Markdown相対リンク、diff検査は成功。Swift/Xcodeの新試験と出荷IPAはこのrunが初回で、未完了。

影響文書の確認snapshotは31478f2。README、CHANGELOG、status、build、mini-apps、compatibility、updating、releasing、ci-boundaries、coexistence-ledger、plan.json、Live/Alarmガイド、新版notes、実機/P2-L記録を確認し、公開版0.8.1と候補0.8.2、実機結果と未解決観測を分けた。patchの範囲確認であり、minor全件監査は行っていない。

## 出荷CIと配布物の確認

CI35075825942は31478f2で成功。通常job3分41秒、状態試験job9分17秒、並列最長9分17秒で15分見込み/30分目標内。共有326件（既知skip2、失敗0）、Records11件、別process incoming200commit/SharedState300update、通常Release・署名・metadata・IPAが成功。新しいtestRealFeatureManagementAndJSONRestorePreserveOtherOwnersは0.984秒で成功し、Actionsの実method passとTEST SUCCEEDEDを確認した。既存native/UI/実機はこのrunで再実行していない。

状態試験のartifactは新しいContinuing-stateディレクトリをupload対象へ加え忘れており、platform/simulator情報だけだった。Actions全ログに実行と成功が残っているため、それを取得・保管して証拠にした。xcresult/result.jsonを取得済みとは記載しない。upload対象を次回用に1行追加し、製品や試験実行は変えず、この保存設定修正だけの再CIは行わない。

通常IPAの全ZIP entry CRC、app/Widget/Shareの既存IDと0.8.2/build12、App Group、コード署名resource、通常Counter Widget kindを確認。実行ファイルとpathに診断owner/試験bundle/Zaikoの混入なし。ZIP64なしの単一IPA入りZIPを作り、外側CRCと内側完全一致を確認した。

- IPA: 2,874,016 bytes、SHA-256 `da0f273226f765297be94f6fa282da42a672e392e47a11fa06f037a503c55de9`。
- ZIP: 2,839,157 bytes、SHA-256 `e44c4edd82ff0b220bbb3afbd5ed5fa1212bc2b161e6c7b2fd033b6b6cff7ac8`。

P2-L累計8run、合計102.217 job分。今回の自動化補強は実行1秒未満、CIはbuild/Simulator準備込み約9分であり、手動組合せの反復を減らす投資として扱う。費用の節約率は未測定。元のLive B観測の原因を特定したとはしない。
