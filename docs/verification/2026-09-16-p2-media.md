# P2-C 音声・撮影の実装/検証記録

状態: 実装中。公開0.8.2/main `52a29ff`から`codex/p2-media-integration`で進行。native build/CI/実機結果はまだない。

## 責任と提出

[共通契約](../delivery/P2-media-contract.md)と音声/撮影の設計提出を親がレビューし、共有資源・既存lifetime/scene/presentation接続・音声付き撮影hookを確定した。音声担当と撮影担当はsol lowの独立CLI実行で、それぞれcore/native adapter/実Feature fixture/試験/guideを実装する。親はhost生成・共有契約・CI・統合レビューを担当する。権限設定の上書き、子CI、短周期監視は行わない。完了時の一回の通知で提出を受け取る。

設計から修正した点は、camera競合をOS任せにしないこと、停止中の並行要求・停止失敗の部分状態を扱うこと、AudioSessionとcaptureの停止callback再入を避けること。iOS26対象とし、background audioは既存Info.plist合成を利用する。

## 親のローカル検証

- `test_media_host.py` 2件: 両Feature/試験の通常hostへの結線、既存identity/version/Featureの保持、試験不足・anchor変更・test誤混入・二重適用を変更前に拒否。
- `test_media_verification.py` 3件: class単位の実試験成功を要求し、欠落/skip/重複/完了なしを拒否。built appのcamera/microphone説明文、background audio、identityを検査。
- 既存`test_continuing_surfaces*.py` 5件も成功。workflow YAML読込みと`git diff --check`成功。

これはPythonの生成/検証処理の試験であり、Swiftの成立や撮影・録音の成功の証拠ではない。

## 初回提出の統合レビュー

音声`cf14eb0`、撮影`c3da8bf`を受領し、親branchへ取り込んだ。実際のtrackedファイルだけを使った一時host生成に成功し、4つのfixture sourceがappへ入り、XCTestはtest targetに分離された。workflow command prefix試験1件とdelivery計画の構造検査（25単位/12境界）も成功。これらもnativeコンパイルの代用ではない。

初回レビューで、撮影のnative開始待ち中にcamera/audioを早期解放する問題、configuration commit前のstartRunning、終了処理の重複待ち後にruntimeが残る問題を確認。親修正`5e46ae2`で開始/停止のjoin、世代と予約の再検査、Feature同意gate、configuration完了後の開始を追加し、担当へ共有した。Swift回帰は追加済み・未実行。

残る修正は各担当へ一括依頼した。撮影は旧delegate結果拒否、native中断/失敗の接続、提示先の所有、Feature同意、成果preview、音声への停止callback接点、実Feature試験。音声は中断後の安全な再開、設定失敗の復旧、Feature停止中の許可遅着、native既定coordinator共有、標準raw値保持、同意・実Feature試験とNow Playing配送終了。修正前の実機投入/CIは行っていない。

撮影のCLI再開操作は実行ポリシーに拒否され、具体的な理由は返されなかった。権限設定を変更せず、親が上記修正を実施した後、既存の音声/撮影タスクをアプリ標準の送信機能で再開した。追加のtask/worktreeは作っていない。両担当は修正版commit後に親へ一回通知する。現在の待機対象は両修正版であり、CI・ユーザー承認待ちではない。

音声review1 `711b252`を受領・取り込んだ。profileのraw値保持、native既定coordinator、復旧状態、録音許可遅着の拒否、Now Playing配送join、録音権限に依存するskip試験の置換が入った。追加レビューでnamed raw値の転記、throwing callbackの呼出し、reactivateの実装/試験のエラー不一致、中断終了前の再開・明示Playと自動復帰の区別、media reset後のSDK object再構築、競合切替中の同意/終了、他owner停止中の解放待ちに残件を確認した。音声review2を一括依頼し、対象SDK定数との比較試験も要求した。撮影review1と並行して修正中。追加の親子レビューは初回指摘の未解消・修正に伴う具体的不整合のためで、CIでの失敗や実機総当たりには転嫁しない。CI投入は引き続き0回。

## CI投入前の未完了事項

1. 両担当の実装を統合し、callback isolation、停止join、競合と許可の失敗、実Featureの管理接続をレビューする。
2. 音声付きcaptureのhookを実audio coordinatorへ接続する。native fixtureと対象testの宣言が揃ったことを確認する。
3. [計画](../delivery/plan.json)のP2-Cに対応する投入reportで、P0/P1再利用source/差分、全入力、対象filter、準備/uploadを含む見積りを固定する。
4. 同一SHAで通常IPAと`media_validation`を並列実行する。診断側は両laneのnative tests→device Release build→署名/IPA検査の一つのjob。step24分budget、job30分上限だが、timeout設定だけで所要時間達成とは扱わない。

`Tools/verify-media.py`は失敗を含む各段階のlog/timing、native xcresult、built Info.plist、result.json、診断IPAを`Media/`へ残す。workflowはこのディレクトリ全体をupload対象とする。実試験が一件でも未実行なら成功とは扱わない。初回CI予算3run、3失敗以内に切り分ける。現時点でCI投入なし。

## 自動試験と実機の分担

状態遷移、互換/非互換要求、片側失敗・停止・取消、遅延callback、世代、他ownerの非初期値保持は自動試験へ置く。実Feature結線も検査し、fakeだけの成功で完了にしない。

実機は実録音/再生、OS中断・代表経路変更、背景継続、Now Playing操作の配送、実camera/文書・コードscan、音声付き撮影の代表切替を対象とする。管理/復元の全組合せ、変更のない上書き/Refreshを繰り返さない。詳細手順はfixtureの実表示と観測方法が揃ってから確定する。

P2-LのLive B単発差分は原因未特定として引き継ぎ、P2-5 partialを変更しない。今回の設計や成功済み自動試験で過去事象の原因が判明したとは扱わない。
