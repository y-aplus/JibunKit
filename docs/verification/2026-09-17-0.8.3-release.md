# 0.8.3出荷照合（未公開）

候補VERSIONは0.8.3/build13、公開安定版PREVIOUSは0.8.2/build12。2026-09-17、文書保存/取消・QR、イヤホン抜去停止、通常IPA復帰後のCounter/Reminder保持を受領した。今回の実機待機は終了し、版変更後のbuild/IPA確認とmain統合・公開へ進む。1.0全体は未完了。

## source別の証拠と未解決事項

- 306874f（0.8.2/build12）: 再生/背景/ロック画面pause/play、Siri後明示再開、実録音/録音再生、写真、動画の背景移動停止と映像/音声を確認。
- a142108（同版）: host全画面提示時のscene誤切断を修正。CI35102558612で共有359件（既存Keychain2skip）、native28件、通常通知UI・通常/診断IPA成功。実機で文書保存/画像/件数/再表示/取消、QR、イヤホン抜去停止、通常版復帰後の既存データを確認。最後の2点はまとめた依頼への「OK」を双方の確認として受領した。
- [実機記録](2026-09-16-p2-media-device.md)と[機械可読証拠](2026-09-16-p2-media-evidence.json)に観測範囲を保持する。許可/失敗/世代/他owner保持の組合せは自動試験であり、全実機マトリクスは再要求していない。
- 写真初回の不明エラーは全文/再現条件不明。後続成功で原因特定としない。旧Live Bの値差分も未解明でP2-5はpartialのまま。

通常IPAはCounter/Reminder/Share。Audio/CaptureはFeature作者向け基盤であり、診断Featureを通常登録へ加えない。P2-1/P2-2の採用通常範囲は完了、AR/高度capture/全機器/全OSの互換性とは区別する。

## 版変更後の検証境界

実機確認後に進める製品差分は本体/Widget/Shareの0.8.3/build13とCIの期待値だけ。Sources・Tests・Modules・Tools・Packageはa142108と同じであることをcommit差分で検査する。既存native28件、通知UI、実機の成功は対象sourceを保持して再利用し、版変更だけで繰り返さない。

追加1run、通常jobのみ。`build-ios.yml ref=codex/p2-media-integration simulator_tests=false media_validation=false feature_validation=false records_validation=false`、その他既定。shared/独立Feature/build requirements、通常Release/metadata/署名/IPAを既存workflowで検証する。直近の同範囲35090872646通常jobは3分42秒。setup/upload込み10分見込み、既存job上限45分。P2-C全体は既存7runに追加した8run目で、今回の1runは実機後の版変更に伴う出荷物検証である。CI成功後にIPA全entry CRC、3bundleのID/version/build、診断非混入と署名構造を照合して公開する。

## 文書レビュー

patchの影響範囲を確認し、minor全文書レビューと偽らない。README/CHANGELOG、status、build/compatibility/updating/mini-apps/releasing、優先実装、台帳/plan、Audio/Captureガイド、実機/証拠、新版notesを同期。積み重なった旧版の現在形intro、台帳のAudio/Capture未確認表記、ガイドの過大な手動マトリクスを改める。過去release/tag/観測sourceは履歴として保持する。候補固定commitと出荷runは完了後に記録する。ignored Zaikoと元workspaceの未commit内容には触れない。
